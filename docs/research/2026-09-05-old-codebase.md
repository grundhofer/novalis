# old-codebase

TOOLS: Local read-only tools only: Bash (cat/sed/grep/wc/find/stat/gzip, git, gh CLI authenticated against github.com/grundhofer/novalis). WebSearch and WebFetch existed as deferred tools but were NOT used — the task was local archaeology and I did not verify any current library versions, Google/Microsoft OAuth rules, Drive/Graph API constraints, or CodeMirror/tauri-specta release state; every such statement above is marked ASSUMED. No files were modified; the only write was a scratchpad copy of the perf-commit grep.

## SUMMARY
The original novalis (/Users/sgrundhoefer/Projects/novalis, 466 commits on main from 2026-05-24 to 2026-08-16, all by the owner plus dependabot, 262 commits carrying Claude-Session trailers) grew from a notes+editor M1 into a 38k-line "beat Obsidian" program: AI (Anthropic/OpenAI/local ONNX embeddings, RAG, entity graph), voice capture with whisper.cpp, iroh P2P E2E sync, git sync via libgit2+vendored OpenSSL, calendar with Google/Outlook OAuth, Notion/ENEX import, docx/HTML export, PDF annotation, canvas, plugins, query engine, block refs, transclusion, mermaid, KaTeX, 34 feature flags, 4 locales. That is exactly why it got heavy: Cargo.lock has 910 crates (46 *-sys crates incl. whisper-rs-sys, ort-sys, libgit2-sys, openssl-src, iroh/quinn), pnpm-lock has 666 packages, the July release binary is 40 MB (18.8 MB dmg), and the current frontend dist eagerly loads 4.7 MB raw / 1.31 MB gzip because the 3.09 MB mermaid chunk and the 670 KB aiStore chunk are modulepreloaded despite the manualChunks intent. The git history contains a dense July-2026 perf campaign whose commit messages are a textbook of what NOT to do in the rewrite: one engine Mutex holding the SQLite connection across file IO (one slow OneDrive hydration froze every command), a full index rebuild on every vault open (540 ms to 5 ms after the mtime scan), ~7 autocommit fsyncs per note during rebuild, TipTap re-rendering the whole shell on every transaction, decoration plugins rescanning the whole document per keystroke (16 ms per keystroke on a 2000-block doc, 191x after the fix), synchronous Tauri commands blocking the main thread (OAuth polling for 180 s, native dialogs deadlocking), watcher echo storms from the app's own writes, and a debounce window where an external change silently discarded typing. The pieces worth lifting nearly verbatim are small and well-tested: oauth.rs (PKCE S256 + loopback listener + refresh-token preservation, 479 lines with unit tests), secrets.rs (keyring, service "app.novalis"), vault/fs.rs (vault_rel/vault_note_rel path guards, write_atomic tmp+fsync+rename, is_cloud_placeholder size>0 && blocks==0, case-only rename handling), conflict/mod.rs (OneDrive/Dropbox conflict-copy regexes + keep original/conflict/both), watcher.rs (notify-debouncer 300 ms, generation counter, self-write suppression), settings.rs (atomic read-modify-write settings.json), engine.rs (poisoned-mutex recovery, CommandError kinds), index/schema.rs (disposable SQLite cache with user_version, WAL, FTS5 unicode61, incremental mtime scan), and the tauri-specta bindings pipeline with its CI drift gate. The docs worth carrying over are docs/decisions/0001-licensing.md (AGPL-3.0-only plus section-7 plugin and app-store exceptions, prospective only, contributor inbound grant in CONTRIBUTING.md, LibXDiff reasoning, trademark follow-up), the README no-telemetry stance with its per-feature outbound-connection table, RELEASING.md (tag -> draft release, release.yml reuses ci.yml via workflow_call as a gate, xa11y UI gate, build-provenance attestations, SHA256SUMS, cargo-vendor source tarball for GPL-2.0 section 3), deny.toml/.cargo/audit.toml/dependabot.yml patterns, and the i18n pipeline (i18next-parser --fail-on-update gate, catalog parity vitest, eslint-plugin-i18next, pseudo-locale). Two design points collide with the new brief and must be decided by the owner: the old Kanban wrote @status(...) annotations back INTO note lines (tasks/index.rs:506-540), whereas the brief demands Kanban content outside notes; and the old editor is a TipTap/ProseMirror WYSIWYG that cannot serve as a Sublime-like editor for arbitrary text file types, while its Markdown round-trip was a recurring data-loss source (table shredding, frontmatter scalar overwrite). Repo state: github.com/grundhofer/novalis is PUBLIC, default branch main = origin/main = a00edc7, protected by ruleset 20518201 (no deletion, no force-push, PR required, 4 required status checks, zero bypass actors), tags v0.1.0/v0.2.0 (MIT) and v0.2.1-rc2 on the remote, releases v0.2.0 (Latest) and v0.2.1-rc2 (pre-release, not draft), 18 remote branches, one unmerged fix branch (fix/oauth-client-id, 1 commit). The safest orphan procedure is: push legacy branch + non-v tag first, push the new tree as a non-default branch, switch the GitHub default branch (the ruleset follows ~DEFAULT_BRANCH), delete/rename, and edit the required-check names — details in the recommendations. I did not use web tools; every claim below is from files read or commands run locally, except items explicitly marked ASSUMED.

## FINDINGS

### F1. Workspace shape and size of the original novalis  [verified]
Cargo workspace (resolver 2) with 3 members: crates/novalis-core (28,206 LOC Rust, 22 modules), crates/novalis-extension (stub, 1 file), apps/desktop/src-tauri (11,183 LOC). pnpm workspace: apps/desktop, apps/desktop/frontend (34,396 LOC TS/TSX; 20,430 in 36 components; 5,599 in 15 zustand stores), packages/editor (5,482 LOC, TipTap 3.30), packages/ui (7-line stub exporting a constant). Version 0.2.1, AGPL-3.0-only, rust-version 1.93 pinned (rust-toolchain.toml) because specta 2.0.0-rc.25 uses core::fmt::from_fn. Release profile: panic=abort, codegen-units=1, lto=true, opt-level="s", strip=true. .cargo/config.toml forces MACOSX_DEPLOYMENT_TARGET=10.15 solely for whisper.cpp's std::filesystem.
_evidence: /Users/sgrundhoefer/Projects/novalis/Cargo.toml, package.json, pnpm-workspace.yaml, rust-toolchain.toml, .cargo/config.toml; wc -l over crates/novalis-core/src, apps/desktop/src-tauri/src, apps/desktop/frontend/src, packages/*/src_

### F2. Core crate module map with LOC (what exists, what to drop)  [verified]
index/ 7,978 (blocks 456, entities 1,159, events 320, links 698, properties 740, query 1,583 = AST+parser+SQL compiler, schema 209, search 1,132, vectors 1,606 HNSW); git/ 2,439; vault/ 2,229 (fs 940, frontmatter 482, config 371, canvas 367, stats 61); tasks/ 2,054 (index 1,091, service 710, nldate 223); models/ 1,997 (preferences 763!, note 268, ai 350); ai/ 1,619 (rag, sse, action); sync/ 1,581 (P2P crypto/manifest/protocol/ticket); import/ 1,480 (notion, enex); notes/ 1,424; calendar/ 1,153; review 457; trash 434; pdf 429; export 389 (docx/HTML); versions 382; plugins 363; conflict 360; templates 193; change 117; media 82; tour.rs 459; help_demo.rs 485. For the rewrite's scope (notes + text editor + kanban + cloud-folder sync) only vault/fs, vault/frontmatter, vault/config (simplified), index/schema+search (simplified), change, conflict, trash, notes (simplified), versions (optional) are relevant: roughly 4,500 of 28,206 lines.
_evidence: find crates/novalis-core/src -name '*.rs' | xargs wc -l; crates/novalis-core/src/lib.rs; crates/novalis-core/Cargo.toml_

### F3. Tauri shell: 158 commands in one flat file, plus AI/voice/sync/git threads  [verified]
apps/desktop/src-tauri/src: commands.rs 2,161 lines with 125 #[tauri::command] fns; ai/commands.rs 20, ai/entities.rs 5, voice/commands.rs 8 (158 total). lib.rs (414 lines) declares the whole surface once in specta_builder() (collect_commands!/collect_events! + 3 exported constants) and is the single source for bindings. Startup (run()): in debug, export_bindings() rewrites frontend/src/ipc/bindings.ts on every launch; plugins log (stdout + OS log dir), dialog, opener, notification; managed state AppEngine (Mutex<Option<Engine{db: rusqlite::Connection, vault_path, data_dir}>>), AiRegistry, LocalEmbedderCache; setup sweeps stale voice recordings synchronously, then reopens the last vault on a background std::thread via commands::open_vault_impl. open_vault_impl: ensure dir -> feature-flag stamp migration -> asset-protocol scope allow_directory (never revoked; audit finding) -> data_dir = app_data_dir/vaults/<vault_key> -> legacy trash migration -> schema::open_db -> incremental_index_with_progress (or full build after legacy migration) -> set engine -> settings save + recent -> spawn watcher thread + autocommit thread (shared generation counter) -> emit reindexed-event. Quit hooks RunEvent::Exit for commit-on-quit with a 5 s bound (bg.rs run_with_timeout). Tauri capabilities: core:default, core:window:allow-destroy, dialog:default, notification:default; strict CSP; dragDropEnabled:false; window 1280x800 min 720x480; identifier com.novalis.desktop; bundle.resources ships licenses/, THIRD-PARTY-NOTICES.md, LICENSE, COMMERCIAL-LICENSE.md.
_evidence: apps/desktop/src-tauri/src/lib.rs, main.rs, engine.rs, bg.rs, commands.rs:169-265, tauri.conf.json, capabilities/default.json; grep -c '#[tauri::command]'_

### F4. IPC/bindings mechanism (tauri-specta) — lift the pattern  [verified]
tauri-specta = "=2.0.0-rc.25" and specta = "=2.0.0-rc.25" pinned equal (comment: must share the same rc); specta-typescript 0.0.12. Builder<tauri::Wry>::new().commands(collect_commands![...]).events(collect_events![...]).constant(...).dangerously_cast_bigints_to_number(). export_bindings() writes ../frontend/src/ipc/bindings.ts (92 KB, 2,201 lines) via specta_typescript::Typescript::default(); regenerated by `pnpm gen:bindings` = `cargo run -p novalis-desktop --example gen_bindings` (examples/gen_bindings.rs, 6 lines). CI gate (ci.yml): run gen:bindings then `git diff --exit-code` on bindings.ts (Linux only, Windows line endings flake). Frontend wrapper ipc/api.ts (328 lines) unwraps `{status:'ok',data}|{status:'error',error:CommandError}` into thrown NovalisError{kind,message}. CommandError{kind,message} kinds used: internal, noVault, notFound, alreadyExists, badRequest, oauthConfig, oauthDenied, oauthTimeout, notConnected. Events are structs deriving tauri_specta::Event (ReindexedEvent, NoteChanged{path}, NoteDeleted{path}, ConflictDetected{path}, GitConflictDetected, AiStream*, AiEmbedProgress) emitted with app.emit("note-changed", ...). Whether rc.25 is still the current tauri-specta release was NOT verified (no web lookup).
_evidence: apps/desktop/src-tauri/Cargo.toml, src/lib.rs specta_builder()/export_bindings(), examples/gen_bindings.rs, apps/desktop/frontend/src/ipc/api.ts, bindings.ts head, .github/workflows/ci.yml 'Verify IPC bindings'_

### F5. i18n implementation — pipeline worth keeping, scope worth cutting  [verified]
i18next ^26.3.0 + react-i18next ^17.0.8. lib/i18n.ts initialises synchronously at module load (imported first in main.tsx) with ALL catalogs statically imported: 4 locales (de, en, es, fr) x 15 namespaces (ai, calendar, common, conflict, editor, help(lazy), links, onboarding, pdf, settings, sidebar, tasks, today, trash, vault, versions); en JSON totals 64,799 bytes, each locale dir 100-112 KB. Keys referenced as t("ns:dotted.key"); typed keys via types/i18next.d.ts from en only. Tooling: `pnpm i18n:extract` (i18next-parser 9.4) and `pnpm i18n:check` = `i18next --fail-on-update` as a CI gate; eslint-plugin-i18next forbids hardcoded UI strings (CI 'Frontend lint'); locales/__tests__/catalogs.test.ts enforces key parity, identical {{vars}}/markup and no empty values across locales; a dev-only pseudo-locale en-XA (accented + bracketed) to spot un-wrapped strings; dates via Intl in lib/datetime.ts. Documented in src/locales/README.md.
_evidence: apps/desktop/frontend/src/lib/i18n.ts, src/locales/README.md, package.json scripts, .github/workflows/ci.yml, du -sh src/locales/*_

### F6. Settings/preferences model — three tiers plus localStorage, with 34 feature flags at the end  [verified]
Tier 1 OS-level `settings.json` in app_config_dir (settings.rs, 229 lines): last_vault, recent_vaults (cap 10, dedup), ai_connections, ai_embedding; every write is a read-modify-write under a static Mutex and an atomic tmp+rename. Tier 2 vault-level `<vault>/.novalis/config.json` (vault/config.rs) holding models/preferences.rs `Preferences{prefs_version, task_view{default_mode, kanban_columns:Vec<KanbanColumnDef{id,title}>, task_creation{strategy,inbox_path}, project_colors}, file_tree{sort_by,sort_dir,folder_colors,item_order}, appearance{theme,accent,font_size,density}, editor{autosave_ms,serialize_ms,spellcheck,default_reading_mode,ambient_ai}, calendar{...}, general{default_app_view}, git{enabled,author_name,author_email,auto_commit_secs}, features: 34 bools, saved_queries}`; try_read_preferences errors loudly on malformed JSON (a silent default once caused the user's file to be overwritten), written via write_atomic. Tier 3 OS keychain (secrets.rs). Plus localStorage for sidebar prefs and right-rail panels. Frontend settingsStore.ts patches in memory, debounced persist with RMW, flushPending() wired to onCloseRequested; fileTree block is owned by vaultStore and settingsStore must never write it — a documented cross-store race. Lesson: 3-4 stores of state with hand-maintained ownership rules and mirrored serde defaults on both sides (resolveFeaturePrefs comment: 'keep the two lists identical or the two sides disagree').
_evidence: apps/desktop/src-tauri/src/settings.rs, crates/novalis-core/src/vault/config.rs, crates/novalis-core/src/models/preferences.rs (grep of struct fields), apps/desktop/frontend/src/stores/settingsStore.ts:1-70_

### F7. oauth.rs — PKCE loopback flow is liftable, but it was calendar-only and needs re-scoping for Drive/OneDrive  [verified]
apps/desktop/src-tauri/src/oauth.rs (479 lines): providers google (auth https://accounts.google.com/o/oauth2/v2/auth, token https://oauth2.googleapis.com/token, scope calendar.readonly, extra access_type=offline&prompt=consent) and outlook (login.microsoftonline.com/common/oauth2/v2.0/{authorize,token}, scope 'Calendars.Read offline_access'). Flow: TcpListener 127.0.0.1:0 -> redirect http://127.0.0.1:<port> -> PKCE verifier = two uuid simple strings (64 chars), challenge = base64url-nopad(SHA256) -> state uuid -> open system browser via tauri-plugin-opener -> non-blocking accept loop with 150 ms sleep and 180 s deadline, parses first request line, answers a static HTML 'Connected' page, checks error/state/code -> reqwest::blocking token exchange -> Tokens{access, refresh, expiry} stored as JSON in keychain account `oauth:<provider>`; access_token() refreshes 60 s before expiry and keeps the previous refresh token when the response omits it (Google behaviour). Client id comes from keychain `oauth-client:<provider>` (user-pasted in Settings) or env NOVALIS_GOOGLE_CLIENT_ID / NOVALIS_MS_CLIENT_ID; the last commit 8bb0ae5 (unmerged branch fix/oauth-client-id) exists because env vars are unreachable for a Finder-launched app so 'Connect Google/Outlook were dead in every installer'. 6 unit tests cover PKCE and token parsing. Quality: good, small, no secrets in the binary. Caveats: blocking reqwest inside a spawn_blocking command; raw HTTP parsing with an 8 KB buffer; no unit test for the listener; Tokens deliberately has no Debug. For Drive/OneDrive file sync the scopes/endpoints/verification requirements are different and were NOT researched here (ASSUMED: Google drive.file scope and Microsoft Files.ReadWrite; Google's OAuth app verification rules for sensitive scopes unverified).
_evidence: apps/desktop/src-tauri/src/oauth.rs (full read); git log main..fix/oauth-client-id (8bb0ae5)_

### F8. secrets.rs — liftable as-is  [verified]
143 lines. One API set/get/delete keyed by account string (`ai:<id>`, `oauth:<provider>`, `oauth-client:<provider>`, `git:<vault>`); blank value deletes. Desktop backend = keyring 3 with per-OS features (apple-native / sync-secret-service+crypto-rust / windows-native), service name KEYRING_SERVICE = "app.novalis"; delete tolerates NoEntry. Android backend = JSON file in app-private dir (drop). No unit tests on purpose (needs a real OS store; headless Linux CI has no secret-service).
_evidence: apps/desktop/src-tauri/src/secrets.rs; apps/desktop/src-tauri/Cargo.toml keyring target sections_

### F9. vault/fs.rs — the most valuable reference file: path guards, atomic write, cloud placeholders, case-only renames  [verified]
940 lines, pure functions over &Path with ~20 tests. vault_rel(base, rel): rejects absolute paths and any ParentDir/RootDir/Prefix component lexically (no canonicalize, so works for not-yet-existing targets); vault_note_rel additionally rejects hidden components and non-.md so the note API can never reach .novalis/, .git/config or plugin dirs. write_atomic(path, contents): same-dir dot-prefixed temp `.<name>.<uuid>.tmp` -> write -> sync_all -> rename, temp removed on error; dot prefix keeps walker/watcher/index from seeing it. is_cloud_placeholder(meta) on unix = len()>0 && blocks()==0 (comment: on APFS even a 1-byte real file has a block); non-unix returns false (so no Windows detection). build_summary returns a NoteSummary{cloud_only:true} from filename+mtime without reading placeholders; list_folders builds the tree from index summaries without touching files. move_note/move_folder allow case-only renames on case-insensitive FS via is_same_file (dev+ino) while rejecting real collisions. write_note fails loud on malformed frontmatter (strict parser) — added after a data-loss bug. Symlink-blindness of vault_rel is a known audit finding (reads follow symlinks out of the vault). Whether blocks()==0 holds for macOS File Provider (OneDrive/Google Drive on macOS 26) dataless files is ASSUMED from commit context ('OneDrive-synced vault'), not tested here.
_evidence: crates/novalis-core/src/vault/fs.rs lines 1-140, 225-335, 411-460 and test names; docs/plans/AUDIT_FINDINGS.md:497_

### F10. conflict/mod.rs — cloud conflict copies: detection + 3 resolutions; regexes are narrow  [verified]
360 lines. Patterns: `^(.+)\s+\(\d+\)(\.md)$` (name (1).md), `^(.+)-DESKTOP-[A-Z0-9]+(\.md)$` (OneDrive device suffix), `^(.+)\s+\(.+'s conflicted copy[^)]*\)(\.md)$` (Dropbox). list_conflicts walks the vault skipping dot-dirs; conflict_diff reads both sides capped at 1 MiB; resolve_conflict keep=original|conflict|both (both renames to `Foo (from sync YYYY-MM-DD HHMM).md`) and updates the index via change::reindex_path/remove. The watcher emits conflict-detected using a matching regex. Frontend ConflictModal + conflictStore (32 lines). Gaps: OneDrive's current suffix is `-<DeviceName>` not only DESKTOP-*, and Google Drive for desktop naming was never covered (ASSUMED; not verified against vendor docs). Lift the structure; re-derive the patterns from real OneDrive/Google Drive samples.
_evidence: crates/novalis-core/src/conflict/mod.rs lines 1-200; apps/desktop/src-tauri/src/watcher.rs conflict_re_

### F11. watcher.rs + change.rs — small, correct after two fixes; lift with the self-write suppression  [verified]
watcher.rs 122 lines: notify 8 + notify-debouncer-mini 0.7 (300 ms) on a std::thread; a global AtomicU64 WATCH_GEN ties the thread to the vault that started it (opening another vault bumps it and the stale thread exits on its 2 s recv_timeout). Filters: .md only, skip hidden components (.novalis), skip ~ temp names, skip paths in commands::is_recent_self_write (Mutex<HashMap<path,Instant>>, 2 s window, registered by every write command; folder ops register the subtree) — added in 004c8a5 because every app write echoed a reindex + UI event ('event storm on bulk folder operations'). Then takes the engine lock only for change::reindex_path_with_opts (feature flags resolved before the lock) and emits note-changed / note-deleted / conflict-detected outside the lock. change.rs (117 lines) is the single funnel for 'a note at this path changed' used by watcher, rescan and mobile; it also stamps mtime for the incremental scan. Frontend side d7d7406 closed a data-loss window: an external note-changed arriving inside the ~200 ms serialize debounce auto-adopted disk content and discarded typing; fix consults the pane flush registry (pendingPath) and routes to the conflict banner instead.
_evidence: apps/desktop/src-tauri/src/watcher.rs, crates/novalis-core/src/change/mod.rs, git show 004c8a5, d7d7406_

### F12. Note model + frontmatter — keep the shape, drop properties/relations/rollups/graph types  [verified]
models/note.rs (268 lines): Note{path,title,content(raw incl. frontmatter),frontmatter,word_count,properties}, NoteFrontmatter{title?,tags,aliases,created,modified,pinned, #[serde(flatten)] extra: serde_json::Value (unknown keys preserved verbatim, #[specta(skip)])}, NoteSummary{path,title,folder,tags,aliases,created,modified,pinned,word_count,task_total,task_completed,cloud_only}; plus PropertyValue/NotePropertyEntry/RelationRef/Rollup*/Graph*/Embed*/Block* types for dropped features. frontmatter.rs (482 lines): gray_matter 0.3 (fallible parse) with a LENIENT reader for indexing and parse_frontmatter_strict for the write path, serialize via serde_yaml 0.9 (unmaintained per audit; audit LOW finding), update_modified stamps the modified field on every save, extract_title (frontmatter > first H1 > filename), OnceLock-hoisted regexes for #tags (skips headings and inline code). The demo vault format the brief mentions (YAML frontmatter + [[wikilinks]] + @due) matches this model.
_evidence: crates/novalis-core/src/models/note.rs (full), crates/novalis-core/src/vault/frontmatter.rs (outline), docs/plans/AUDIT_FINDINGS.md:599_

### F13. Search index — disposable SQLite cache pattern is right; keep 3 tables, not 9  [verified]
index/schema.rs: rusqlite 0.40 bundled; open_db sets PRAGMA journal_mode=WAL, busy_timeout 5 s, synchronous=NORMAL; PRAGMA user_version vs SCHEMA_VERSION (=10) -> on mismatch drop+recreate all cache tables (no migrations ever); vectors/entities live in separate tables that survive the drop. Tables: note_meta (path PK, title, folder, tags JSON, aliases JSON, created, modified, size, word_count, pinned, task_total, task_completed, cloud_only, mtime), notes_fts = FTS5(title, content, tags, path, tokenize='unicode61'), tasks, links, note_properties, note_relations, block_index, block_refs, events. search.rs: search() binds the user query as a quoted phrase (never interpolated), folder/tag filters via LIKE ... ESCAPE, ORDER BY rank LIMIT 50, snippet(); quick_search = LIKE over title/path/aliases LIMIT 20; build_index runs in ONE unchecked_transaction after cde9da7 (~7 autocommit fsyncs per note before); incremental_index (8cab4c5) stats every note, reindexes only changed mtimes, drops vanished ones: 2,000 unchanged notes 540 ms -> 5.1 ms release; a 4.6k-note full rebuild still measured ~2 min (2d9fb96) i.e. ~26 ms/note with all ingest passes — the rewrite should keep per-note work to frontmatter + FTS only. list_summaries serves the tree from the DB with zero disk reads. The DB lives in app_data_dir/vaults/<key>/db/notes.db, never in the vault.
_evidence: crates/novalis-core/src/index/schema.rs, index/mod.rs, index/search.rs:1-110,398-476; git show 8cab4c5 cde9da7 2d9fb96 d701aee_

### F14. Trash and version history designs  [verified]
trash/mod.rs: soft delete into `<vault>/.novalis/trash/` with a `.meta` sidecar (original path), id = `%Y%m%d_%H%M%S_<uuid>_<name>`; lives in the vault so it syncs; hidden dirs are excluded from index/watcher. versions/mod.rs: on each save snapshot the PREVIOUS on-disk content under `<data_dir>/versions/<encoded-path>/<timestamp>.md`, MAX_VERSIONS 50, MAX_AGE_DAYS 30, MIN_INTERVAL_SECS 180 (throttled checkpoints, de-duplicated) — app-data only, vault stays clean Markdown. Both are simple and could be lifted; versions is a feature beyond the brief (needs owner decision).
_evidence: crates/novalis-core/src/trash/mod.rs:1-50, crates/novalis-core/src/versions/mod.rs:1-40_

### F15. Old Kanban wrote status INTO the notes — the opposite of the new brief  [verified]
Tasks are markdown checkboxes with inline annotations `@due(...) @priority(...) @status(id) @repeat(...) @start @remind @project @epic` (tasks/mod.rs doc); the Kanban board's column move calls tasks::service::set_status which rewrites the `@status(...)` token in the source line and write_atomic's the whole note (tasks/index.rs:506-540, 707); kanban_columns live in vault prefs (KanbanColumnDef{id,title}); TasksView.tsx is 874 lines, board virtualised (fb03b73). The tasks table in SQLite has 17 columns. The rewrite's Kanban must instead be a separate store (e.g. a JSON file under `<vault>/.novalis/` so it syncs with the vault) with cards linking to notes by path — which raises a rename problem the old code solved only via move_note + reindex; the new design needs link re-pointing on rename or stable note ids (frontmatter id) — owner decision.
_evidence: crates/novalis-core/src/tasks/mod.rs, tasks/index.rs:506-540, tasks/service.rs:112-174, models/preferences.rs:36-90, models/task.rs_

### F16. Editor: TipTap 3 WYSIWYG with a fragile Markdown round trip; mismatch with 'Sublime-like text editor' brief  [verified]
packages/editor: @tiptap/* ^3.30.0 (migrated from 2.27.2 in 8103a9e, Aug 2026), tiptap-markdown ^0.9.0 for serialization, custom MarkdownTable/MarkdownText serializers, extensions WikiLink, Callout, Math (katex 0.18.4), Embed, BlockRef, Find, SlashCommand, Tag/WikiLink/BlockRef suggestions (@floating-ui/dom), MermaidCodeBlock (mermaid 11), lowlight lazy grammars, SuggestRewrite (AI). NovalisEditor.tsx 668 lines. Tests: markdownRoundTrip.test.ts golden corpus (340 lines), extensionStack.test.ts invariants (385), incrementalDecorations.test.ts, mathRender.test.ts. Data-loss history tied to the WYSIWYG<->Markdown conversion: ed830e4 'one Enter in a table cell replaced the whole table with [table]', v0.2.1-rc1 tag message 'pipe in a table cell shredded the row on reload' and 'frontmatter scalar overwrote first section on save', 84e3c2e frontmatter integer values dropped. Perf lessons if TipTap is kept: shouldRerenderOnTransaction:false + useEditorState selectors (e3d73f3), map DecorationSets through transactions and rescan only changed top-level blocks (e9ff345: 16.07 ms -> 0.084 ms per keystroke on 2,000 blocks), skip rebuilds on selection-only transactions and give widgets stable keys (db96d61), lazy grammar registration (70aeb81: ~149 KB out of the eager bundle). A ProseMirror WYSIWYG cannot edit arbitrary text/code file types, which the brief requires; a code-editor engine (CodeMirror 6 is the obvious candidate — ASSUMED, version not verified) with Markdown syntax highlighting fits 'Sublime-like' better. Owner decision.
_evidence: packages/editor/package.json, wc -l packages/editor/src/*, git show e3d73f3 e9ff345 db96d61 70aeb81 ed830e4, git show -s v0.2.1-rc1_

### F17. Frontend structure: zustand god-store and 1,000+-line components  [verified]
stores: vaultStore.ts 1,362 lines (80 members: vault path, folder tree, per-pane open notes, save lifecycle, external-change detection, sidebar state, nav history, folder colors/order, flush registry, global error reporting; imported by 40 files), uiStore 482, aiStore 429, taskStore 334, pluginStore 236, settingsStore 230, voiceStore 222, vaultChatStore 186, calendarStore 123, gitConflictStore 120, canvasStore 85, agendaStore 69, conflictStore 32, pdfStore 30, keymapStore 29. Components: Sidebar.tsx 1,569 (decomposed into ~22 sub-components), EditorPane.tsx 1,256 (single component, 10 useEffects), CanvasView 1,048, TasksView 874, PdfViewer 776. Note cache + in-flight dedup + lastRequest dedup + writeInFlight maps live at module scope in vaultStore (the comment says a OneDrive online-only read hydrates over the network ~1 s, so cache + prefetch-on-hover is what made opening feel instant). Deps: react 19, zustand 5, @tanstack/react-virtual (tree + kanban virtualised in 0485215/fb03b73), lucide-react, tailwindcss 4 via @tailwindcss/vite, vite 8 (rolldown), vitest 4, typescript ~6.0.3, @fontsource-variable/inter, d3-force, pdfjs-dist 6.
_evidence: wc -l apps/desktop/frontend/src/stores/*.ts, src/components/*.tsx; src/stores/vaultStore.ts:1-45; apps/desktop/frontend/package.json; docs/plans/AUDIT_FINDINGS.md:740-764_

### F18. Dependency weight: 910 crates / 46 *-sys crates / 666 npm packages; the heavy stacks by name  [verified]
Cargo.lock: 910 [[package]] entries, 46 '*-sys' crates. Present: tauri 2.11.5, iroh 1.0.3 + quinn 0.11.9 + rustls 0.23.40 (P2P sync), whisper-rs-sys 0.15.0 (compiles whisper.cpp with cmake at build time), ort-sys 2.0.0-rc.13 (ONNX runtime download-binaries; no x86_64-apple-darwin prebuilt => Intel Mac builds impossible per RELEASING.md), fastembed 5.17.4 + tokenizers 0.22.2 + ndarray 0.17.2 + esaxx-rs, libgit2-sys 0.18.5+1.9.4 + openssl-src 300.6.1 (vendored, statically linked), hnsw_rs 0.3.4, docx-rs 0.4.22, zip 8.6, quick-xml 0.41, cpal 0.18.1 (needs libasound2-dev on Linux CI), rusqlite 0.40.2/libsqlite3-sys 0.38.2, webkit2gtk 2.0.2. pnpm-lock.yaml (lockfileVersion 9.0): 666 packages; pnpm-workspace.yaml carries an override forcing katex ^0.18.4 because mermaid 11.16.1 wants ^0.16.45 (measured 511 kB raw / 150.6 kB gzip duplicated otherwise). Local target/ directory is 25 GB. THIRD-PARTY-NOTICES.md is hand-maintained per component (libgit2 GPL-2.0+linking exception, LibXDiff LGPL-2.1-or-later, OpenSSL Apache-2.0, Inter OFL-1.1, EDL-1.0) and was already 16 crates stale once.
_evidence: grep -c '^\[\[package\]\]' Cargo.lock; awk over Cargo.lock for versions; pnpm-lock.yaml packages: section count; pnpm-workspace.yaml; du -sh target; RELEASING.md 'Intel macOS cannot be built'; docs/plans/DEPENDENCY-BACKLOG.md_

### F19. Binary and bundle sizes  [verified]
target/release/novalis-desktop = 40,172,544 bytes and target/release/bundle/dmg/Novalis_0.2.0_aarch64.dmg = 18,806,375 bytes, both built 2026-07-13 (stamped 0.2.0; which of the July features were compiled in was not verifiable from the artifact). Frontend dist (built 2026-09-04, Vite 8/rolldown): 8.2 MB total, 62 JS files = 5,598,868 bytes JS, 99,724 CSS, 1,291,460 fonts (Inter variable 7 subsets + full KaTeX font set). Largest chunks: mermaid 3,092,228; pdf.worker 1,262,398; aiStore 670,593; pdfjs 426,144; settingsStore 275,420; index (entry) 263,172 (65,157 gzip); katex 258,752; react-vendor 189,596; lowlight 139,645. index.html modulepreloads 27 chunks including mermaid, d3-force, aiStore, canvasStore, pdfStore, vaultChatStore: EAGER bytes (entry + preloads + css) = 4,702,353 raw / 1,310,055 gzip. So the manualChunks 'lazy' intent in vite.config.ts is defeated in the current build too (audit finding 1275 said the same for the earlier build: the entry statically imported 20 bindings from mermaid).
_evidence: ls -l target/release, target/release/bundle/dmg; ls -lS apps/desktop/frontend/dist/assets; grep modulepreload apps/desktop/frontend/dist/index.html; summed stat -f%z and gzip -c | wc -c; apps/desktop/frontend/vite.config.ts; docs/plans/AUDIT_FINDINGS.md:1275_

### F20. Perf/complexity commit archaeology — the concrete anti-patterns and their measured cost  [verified]
The requested grep returned 148 commits (many false positives from 'flag'/'change'); the real perf/freeze commits and lessons: d701aee/0543db8 engine Mutex (holding the SQLite Connection) was held across file IO so 'one slow cloud hydration/write could freeze the whole command surface' -> snapshot vault path, do IO off-lock, re-lock for DB only; update_note made async. 8cab4c5 full rebuild on every open -> mtime incremental scan (540 ms -> 5.1 ms on 2k notes). cde9da7 per-note autocommit -> single transaction; 11 task regexes compiled per note -> OnceLock. a8b21ce + 0689877 sync commands ran blocking work on the main thread (oauth_begin polled 180 s; blocking native dialogs = guaranteed main-thread deadlock) -> async + spawn_blocking. 004c8a5 watcher echoed every app write (event storm). d7d7406 external change inside the 200 ms serialize debounce silently discarded typing. e3d73f3/e9ff345/db96d61/70aeb81 TipTap shell re-render per transaction, full-document decoration rescans per keystroke, unkeyed KaTeX widgets re-created per cursor move, 37 grammars in the eager bundle. 0485215/fb03b73/483e6c8 sidebar tree and kanban rendered every row -> virtualisation. fcfb2d9 retrieve_related rebuilt an HNSW graph on every call (871 ms at 5k chunks vs 7 ms brute force). 9ecb05d/0c5c3fd lazy settings/AI panels and react vendor split (eager index 1,583 kB -> 1,175 kB). 2d9fb96 progress UI because a 4.6k-note reindex took ~2 min and looked like a hang. 13619a0 settings persists were silently dropped -> flushPending. Root causes are structural: one global lock + one SQLite connection, a 158-command surface, eager everything, and features layered on a WYSIWYG editor.
_evidence: git log --oneline --all -i --grep=perf ... (saved to scratchpad/perf-commits.txt); git show --stat for d701aee 8cab4c5 e3d73f3 e9ff345 db96d61 004c8a5 d7d7406 a8b21ce 0689877 9ecb05d 0c5c3fd 70aeb81 fcfb2d9 cde9da7 0485215 0b696fc 2d9fb96 d1ef53a_

### F21. Licensing decision (ADR 0001) — carry over, but re-check which exceptions still apply  [verified]
docs/decisions/0001-licensing.md (accepted 2026-08-09, tracked in git): AGPL-3.0-only (not -or-later), plus AGPL section 7 additional permissions in LICENSE for (1) plugins talking only through the documented Plugin API (may be proprietary/sold) and (2) app-store distribution (waives section 10). Rejected: PolyForm Noncommercial (forbids freelancers' paid use), PolyForm Perimeter and n8n SUL (not OSI, close Debian/Fedora/Homebrew core), BSL/FSL (delayed opening), MIT status quo. Deciding argument: statically linked LibXDiff (LGPL-2.1-or-later inside libgit2) makes a permissive outbound license require a relinking kit; under AGPL the obligation disappears via LGPL-3.0/GPL-3.0/AGPL section 13. Consequences recorded: prospective only (v0.1.0 and v0.2.0 stay MIT forever; 0 forks at the time), contributions carry an inbound grant in CONTRIBUTING.md ('must not be removed'), never patch vendored libgit2 C, source must ship from the same place as binaries (GPL-2.0 section 3), a store build must resolve LibXDiff separately, trademark registration for 'Novalis' is an open follow-up (Brainlab conflict noted, clearance search not done). COMMERCIAL-LICENSE.md offers negotiated commercial terms (sole copyright holder). If the rewrite drops libgit2 and plugins, the GPL-2.0-section-3 tarball and the plugin exception become moot, but AGPL section 6 corresponding-source still applies; RELEASING.md flags that npm packages are not vendored into the source tarball as an unresolved AGPL question.
_evidence: docs/decisions/0001-licensing.md (full), COMMERCIAL-LICENSE.md (full), RELEASING.md 'License obligations', THIRD-PARTY-NOTICES.md head, LICENSE size 45,819 bytes_

### F22. No-telemetry stance and outbound-connection table — carry over verbatim in spirit  [verified]
README.md:113-135 'Privacy & network': 'no telemetry, no analytics, no crash reporting, and no update check — there is no build in which the app contacts us'; fresh vault with nothing enabled makes zero network requests; a table lists every opt-in feature, the hosts it talks to and what is sent (calendar .ics, Google/Outlook OAuth hosts, AI providers, huggingface.co model downloads, git remote, iroh relay mesh). Audit history: the earlier README claim was 'factually false' because four network paths existed (AUDIT_FINDINGS 241), fixed by making the table exhaustive. The roadmap verified grep -rniE 'telemetry|analytics|posthog|sentry|mixpanel|amplitude' over crates/apps/packages -> zero matches. For the rewrite the table shrinks to: Google Drive / OneDrive (only if in-app login is chosen).
_evidence: README.md:99-135; docs/plans/PUBLIC_RELEASE_ROADMAP.md:283-293; docs/plans/AUDIT_FINDINGS.md:241_

### F23. Release process, signing/notarization state, CI workflows  [verified]
Workflows (.github/workflows): ci.yml 241 lines (push main, pull_request, weekly Monday cron, workflow_call, workflow_dispatch; permissions contents:read; concurrency cancel; matrix ubuntu/macos/windows: pnpm 11 frozen lockfile, frontend build, eslint i18n, i18n:check, pnpm -r test, typecheck, scripts/check-versions.mjs stamp sync, cargo fmt, clippy -D warnings --locked, cargo test --locked, gated P2P loopback test, bindings drift diff; separate 'dependency audit' job: cargo-audit 0.22.2 + cargo-deny 0.20.2 via taiki-e/install-action, `cargo deny --locked check licenses bans sources` over 4 targets, `pnpm audit --audit-level=high`; all actions pinned to SHAs), release.yml 459 lines (on v*.*.* tag + workflow_dispatch; permissions {} at top; jobs gate = ci.yml via workflow_call, ui-gate = e2e.yml, build matrix with tauri-apps/tauri-action v1.0.0 (locked CLI) creating a DRAFT release, prerelease derived from '-' in the tag, actions/attest-build-provenance v4.2.2 per installer, source job: cargo vendor --locked --versioned-dirs + git archive tarball (~166 MB gz) attested and uploaded, SHA256SUMS built+attested, pre-release flag re-asserted), e2e.yml 210 lines (xa11y 0.13.0 drives the real app through AXUIElement/AT-SPI2/UI Automation on 3 OSes; only check that launches the app; e2e/ is deliberately outside the pnpm workspace), bundle-smoke.yml (weekly Ubuntu-only bundle), webkit-atspi-repro.yml (investigation). Artifacts: macOS aarch64 dmg + app.tar.gz only (Intel blocked by ort-sys), Linux deb/AppImage/rpm, Windows msi/exe. Signing: NOT wired — RELEASING.md 'Phase B' lists the exact tauri-action secrets (APPLE_CERTIFICATE, APPLE_CERTIFICATE_PASSWORD, APPLE_SIGNING_IDENTITY, APPLE_ID, APPLE_PASSWORD app-specific, APPLE_TEAM_ID; WINDOWS_CERTIFICATE/_PASSWORD) and the env block in release.yml:163-166 is commented out; users get 'Apple cannot check it for malicious software' and must right-click Open or `xattr -d com.apple.quarantine`; roadmap classes signing as a spend decision, not a launch prerequisite. Updater: not wired (no plugins.updater block) — roadmap says do not bake an endpoint before the repo transfer. Version stamps in 3 files + Cargo.lock; main is protected so bumps go through PRs. Lessons recorded: tauri-action v1 refuses to reuse a published release (delete release object, not just tag), re-runs onto a draft keep old notes/flag, Linux artifact names must match productName case, a failed platform blocks the source tarball (release-blocking).
_evidence: .github/workflows/ci.yml (full), release.yml (grep of key lines), e2e.yml size, e2e/README.md, e2e/package.json, RELEASING.md (full), tauri.conf.json bundle block_

### F24. Dependency-hygiene config worth copying: deny.toml, .cargo/audit.toml, dependabot.yml  [verified]
deny.toml (8.5 KB): [graph] targets x86_64-linux-gnu, x86_64-windows-msvc, x86_64-apple-darwin, aarch64-apple-darwin; [licenses] explicit allow-list of permissive/weak-copyleft ids compatible with conveying under AGPL-3.0-only, own crates as named exceptions; sources restricted to crates.io; bans report duplicate majors; comments stress cargo-deny cannot see vendored C. .cargo/audit.toml: [output] deny=["yanked"] (added after yanked spin crates reached all rc1 assets with green CI), quiet=false (required field), [advisories] ignore list currently EMPTY with dated notes explaining why entries were removed (each ignore needs a reason + revisit condition checkable by a stranger). dependabot.yml: weekly Monday, cargo + npm each with ONE root-scoped entry (member-scoped npm entries duplicated PRs and produced lockfile-less PRs that fail --frozen-lockfile), grouped minor+patch, open-pull-requests-limit 5, github-actions ecosystem too, TypeScript 7 held.
_evidence: deny.toml:1-40, .cargo/audit.toml (full), .github/dependabot.yml:1-40_

### F25. Planning docs: only the ADR is tracked; TIER/AUDIT/ROADMAP plans are gitignored scratch  [verified]
git ls-files docs/ returns only docs/decisions/0001-licensing.md and docs/assets/*.png; /docs/plans/ is ignored by .gitignore:94. The untracked plans (AUDIT_FINDINGS.md 2,068 lines with ~100 severity-tagged findings, PUBLIC_RELEASE_ROADMAP.md 471, DEPENDENCY-BACKLOG.md 862, GIT_SYNC_SPIKE.md 116 in German, TIER1-4_PLAN.md) exist only in the local checkout and would be lost if the directory is deleted. Notable reusable content: AUDIT_FINDINGS security items (vault_rel symlink-blind 497; ICS fetch SSRF/no size cap 513; HTML export unsanitized 475; reveal_in_file_manager bypasses path guard 579; asset-protocol scope never revoked 705; plugins can write .novalis/config.json 681) and the roadmap's 'Do NOT do this' list (no history rewrite; no signing secrets or updater before transfer). TIER4 is the 'Beat Obsidian' program (W1.1 on-device embedder ... W4.4 P2P) that produced the bloat; TIER3 Bet 3 was 'Code signing + auto-updater' (never done). Recommendation: copy docs/decisions/ and, if wanted, the security findings into the new repo as tracked ADRs; leave the rest.
_evidence: git ls-files docs; git check-ignore -v docs/plans/TIER1_PLAN.md (.gitignore:94); wc -l docs/plans/*; docs/plans/AUDIT_FINDINGS.md headings; docs/plans/PUBLIC_RELEASE_ROADMAP.md headings_

### F26. Git sync spike (docs/plans/GIT_SYNC_SPIKE.md) — evidence base if git sync is ever reconsidered  [verified]
2026-06-12 decision doc: git2 0.21 chosen over gix 0.84 (no push) and shell-out (iOS dead end; bundling); HTTPS PAT first, no ssh (avoids OpenSSL on mac/win), merge not rebase, block on conflict, OS keychain for tokens; live GitHub push verified 1.4 s with Cred::userpass_plaintext("x-access-token"); libgit2 re-invokes the credential callback on 401 (4x) so an attempt bound is mandatory; vendored-openssl+vendored-libgit2 required or the binary links Homebrew dylibs (otool-verified). This dragged libgit2-sys + openssl-src (and the GPL-2.0 section 3 source-tarball obligation) into every build. The new brief specifies Google Drive/OneDrive, so this whole stack is a DROP.
_evidence: docs/plans/GIT_SYNC_SPIKE.md (full), Cargo.toml git2 workspace dependency comment, apps/desktop/src-tauri/src/autocommit.rs:1-25_

### F27. Cloud sync in the old app = local synced folder only; no Drive/OneDrive API integration existed  [verified]
All OneDrive/iCloud handling is filesystem-level: placeholder detection (vault/fs.rs), watcher on the local folder, conflict-copy detection, in-vault `.novalis/trash` so trash syncs, versions kept OUT of the vault, app-data index per vault key. OAuth exists only for read-only Google Calendar / Microsoft Graph calendarView. There is no Google Drive API or OneDrive Graph file client anywhere in the tree. So the brief's 'option B' (log in to Google Drive/OneDrive inside the app) is greenfield; the reusable parts are oauth.rs + secrets.rs. Time-sensitive facts (Drive API quotas, Google OAuth verification for drive scopes, Microsoft app registration requirements, macOS File Provider behaviour) were not researched here — no web tools were used.
_evidence: grep over apps/desktop/src-tauri/src/oauth.rs providers; crates/novalis-core/src/vault/fs.rs; trash/mod.rs; versions/mod.rs; README.md privacy table_

### F28. Repo/branch/tag/release state of github.com/grundhofer/novalis  [verified]
Public repo (owner grundhofer, 1 star, 0 forks, licenseInfo 'Other', pushedAt 2026-09-02). Default branch main; local main == origin/main == a00edc7 (2026-08-16, merge of PR #90). Ruleset 20518201 'main protection' on ~DEFAULT_BRANCH: rules deletion, non_fast_forward, pull_request (0 approvals, extra approval for unattributed changes), required_status_checks strict = build (ubuntu-latest), build (macos-latest), build (windows-latest), dependency audit; bypass_actors = [] (nobody, not even the owner, can force-push main). Remote tags: v0.1.0, v0.2.0, v0.2.1-rc2 (v0.2.1-rc1 exists locally only, at 8cd7e1e). Releases: v0.2.0 (Latest, 2026-06-15, MIT era), v0.2.1-rc2 (published pre-release, not draft, 2026-08-16). 18 remote branches: main, chore/release-hygiene, deps/tiptap-3, fix/notion-asset-lookup, fix/oauth-client-id (1 commit ahead of main: 8bb0ae5 fixes dead 'Connect Google/Outlook' in installers — the only unmerged fix), fix/release-macos-arm64, fix/save-path-data-loss, fix/table-cell-blocks, investigate/webkitgtk-atspi, release/v0.2.1-rc1, spike/e2e-xa11y, test/math-rendering, test/pane-flush-contract, test/save-path-guards, 4 dependabot/*. Local-only: planC-capture (2 commits: quick-capture hotkey + tray), pr20, pr20fresh, tier4-wave1, 5 worktree-agent-* branches, ~40 stale remote-tracking refs (run git fetch --prune). Working tree clean; .claude/ is not in the repo .gitignore (relies on a global rule; not verified which). Commits: 466 on main, 15/66/215/170 per month May-Aug, 262 carry 'Claude-Session: https://claude.ai/code/...' trailers (audit flagged as MEDIUM).
_evidence: gh repo view --json ...; gh api repos/grundhofer/novalis/rulesets/20518201; gh release list --json; git ls-remote --heads/--tags origin; git branch --no-merged main; git log main..fix/oauth-client-id; git log --format='%ad' | uniq -c; git log --format=%b | grep -c Claude-Session_

### F29. The feature-flag system is exhibit A for 'ask before adding a setting'  [verified]
models/preferences.rs FeaturePrefs has 34 booleans (ai, ai_meta_suggestions, ai_templates, task_extract, weekly_review, vault_chat, related_notes, entity_graph, block_refs, transclusion, mermaid, math, code_highlight, callouts, tag_autocomplete, outline, today_view, tasks, calendar, plugins, query_engine, daily_notes, reminders, graph_view, properties, backlinks, calendar_sync, ics_subscriptions, canvas, pdf_annotate, voice ...), a migration (ensure_features_stamp, prefs_version) to keep old vaults 'all-on', Settings > Features panel, onboarding picker, and a Feature Guide with demo notes (help_demo.rs 485 lines + tour.rs 459 lines + src/help 997 lines). IndexOptions::for_vault reads config.json to decide ingest passes. That machinery (7 commits 7aa5dfc..9705fd9) exists only because features were never removed.
_evidence: crates/novalis-core/src/models/preferences.rs:212-285, vault/config.rs ensure_features_stamp, index/search.rs IndexOptions, git log --grep 'feat(features)'_

## RECOMMENDATIONS

### R1. KEEP as reference (copy into the new repo under a clearly labelled reference dir or re-implement from these files): apps/desktop/src-tauri/src/oauth.rs, secrets.rs, settings.rs, engine.rs, watcher.rs; crates/novalis-core/src/vault/fs.rs (vault_rel, vault_note_rel, write_atomic, is_cloud_placeholder, is_same_file/case-only rename, build_summary), vault/frontmatter.rs (lenient vs strict parse, extra passthrough), models/note.rs (Note/NoteFrontmatter/NoteSummary only), index/schema.rs + search.rs (open_db pragmas, user_version drop-and-rebuild, FTS5 unicode61, single-transaction rebuild, incremental mtime scan, list_summaries), change/mod.rs, conflict/mod.rs, trash/mod.rs; the tauri-specta builder + gen_bindings example + bindings drift gate; ipc/api.ts unwrap wrapper; the i18n pipeline (i18next-parser --fail-on-update, catalog parity test, eslint-plugin-i18next, pseudo-locale); ci.yml/release.yml/deny.toml/audit.toml/dependabot.yml as templates; docs/decisions/0001-licensing.md, COMMERCIAL-LICENSE.md, the CONTRIBUTING inbound grant, the README privacy table.
These are small (each under 1,000 lines), have tests, encode hard-won fixes (data-loss guards, path escapes, cloud placeholders, echo suppression, poison recovery, atomic settings), and none of them pull heavy dependencies: rusqlite bundled, notify, keyring, reqwest(rustls), sha2, base64, uuid, serde, specta.
_rejected: Forking the old repo and deleting features: the god-store, the 158-command surface, the flag migration machinery and the TipTap round-trip layer are load-bearing everywhere; deleting is more work than restarting with the listed files as reference._

### R2. DROP entirely: ai/*, voice/* (whisper, cpal, hound), sync/* + iroh P2P, git/* + autocommit + libgit2/openssl, calendar/* + ICS + Google/Outlook calendar fetch, import/* (Notion, ENEX, zip, quick-xml), export docx (docx-rs), pdf/* + pdfjs, canvas, plugins + novalis-extension, review, entities, index/vectors (hnsw_rs) + index/query + blocks/block refs + properties/relations/rollups + events, transclusion/embeds, templates, tour/help_demo/Feature Guide, mobile.rs + Android/iOS scripts + MOBILE.md, es/fr locales, FeaturePrefs flags, appearance theme/accent/density prefs, keybindings panel, d3 graph view, mermaid, KaTeX (unless owner asks), lowlight (revisit with the editor decision), the source-tarball cargo-vendor job (only needed while libgit2 is linked), .cargo/config.toml deployment-target override.
Every one of these is outside the brief (notes, Sublime-like text editor, external Kanban, Drive/OneDrive sync, de/en) and together they account for roughly 24k of 28k core lines, ~40 of 46 *-sys crates, and ~4.4 MB of the eager frontend bundle.
_rejected: Keeping them behind feature flags: the old repo proves flags do not reduce binary size, dependency count, CI time or the settings surface._

### R3. Architecture rules for the rewrite derived from the perf history: (1) never hold a lock across file IO — snapshot the vault path, do IO, re-lock for the DB; better, keep the SQLite connection on a dedicated thread/actor with a channel. (2) every Tauri command that touches disk, network or a native dialog is async + spawn_blocking from day one. (3) index = disposable cache; open = walk + stat + incremental reindex; per-note work = frontmatter + FTS only, batched in one transaction, with a progress event. (4) watcher suppresses the app's own writes by path+time and never reindexes under the UI lock. (5) autosave must flush before any navigation and external changes must check the flush registry before adopting disk content. (6) the frontend eager bundle should be one entry + react vendor; verify with `grep modulepreload dist/index.html` in CI, because manualChunks alone did not keep mermaid out. (7) no module-scope singletons that grow into god-stores: separate stores for vault/tree, editor-save, navigation, kanban.
Each rule maps to a measured regression in the old repo (see the perf archaeology finding) that cost a July-2026 campaign of ~25 commits to fix after the fact.
_rejected: Copying the old command layer and fixing it incrementally: the fixes were workarounds for the single-Mutex design (async update_note that re-acquires the lock, recent-writes map), not a redesign._

### R4. Kanban design for the brief: store boards/cards OUTSIDE notes as JSON under `<vault>/.novalis/kanban/` (syncs with the vault via the cloud folder, hidden from the note index like trash) — or in app-data if the owner prefers device-local boards; cards reference notes by vault-relative path plus an optional stable frontmatter `id`; move_note must re-point card links (the old app only had move_note + index reindex). Reuse the old KanbanColumnDef{id,title} shape and the virtualised board rendering idea (fb03b73), not the @status-in-note write-back.
The old tasks/index.rs:506-540 rewrote note lines on every column move, which is the behaviour the brief explicitly forbids, and rewriting notes from the board is also what produced watcher echo storms and external-change conflicts.
_rejected: Sidecar per note (.md.kanban.json) — scatters state across the tree and multiplies conflict copies on cloud sync._

### R5. Sync strategy: ship 'option A' (local OneDrive/Google Drive folder as the vault) first, reusing placeholder detection, conflict-copy detection/resolution, watcher, in-vault trash and app-data index; treat 'option B' (in-app Google Drive / OneDrive login) as a separate spike using oauth.rs + secrets.rs as the starting point, with scopes, endpoints, verification requirements and rate limits researched by an agent with web access before any commitment.
Option A is fully covered by lifted, tested code; option B has zero prior art in the old repo and carries unverified external constraints (Google OAuth app verification for Drive scopes, Microsoft app registration, token lifetimes) that I could not check without web tools.
_rejected: Building option B first: highest risk, no reusable code beyond the PKCE loopback, and the brief says 'if possible'._

### R6. Safest git procedure for 'replace forever' while preserving history. Preconditions: `cd /Users/sgrundhoefer/Projects/novalis && git fetch --prune origin && git status` (clean; main == origin/main == a00edc7). Decide first whether to merge fix/oauth-client-id (8bb0ae5) into main via PR so legacy carries the last fix. Step 1 preserve: `git branch legacy main && git push origin legacy` and `git tag -a legacy-final -m "Last commit of the original Tauri/React/TipTap novalis before the rewrite" a00edc7 && git push origin legacy-final` (tag name deliberately does NOT match v*.*.* so release.yml does not fire). Optionally add a second ruleset on `legacy` with only deletion + non_fast_forward. Step 2 new history: in /Users/sgrundhoefer/Projects/novalisNeo run `git init -b main`, commit the scaffold, `git remote add origin git@github.com:grundhofer/novalis.git`, `git push -u origin main:rewrite` (a new non-default branch; the ruleset only targets the default branch, so this push is allowed). Step 3 switch default: `gh api -X PATCH repos/grundhofer/novalis -f default_branch=rewrite`; the ruleset follows ~DEFAULT_BRANCH, so protection now covers rewrite and old main becomes unprotected. Step 4 retire old main: `git push origin --delete main` (allowed now), then rename: `gh api -X POST repos/grundhofer/novalis/branches/rewrite/rename -f new_name=main` (GitHub rename retargets open PRs and keeps it as default). Locally: `git branch -m main` if needed, `git fetch --prune`, `git branch -u origin/main`. Step 5 ruleset: edit ruleset 20518201 so required_status_checks name the NEW CI job names (or temporarily remove that rule) — otherwise the first PR into the new main can never merge. Step 6 workflows: the old cron in ci.yml and dependabot.yml stop automatically because schedules read only the default branch; close the 4 open dependabot PRs and the stale PRs; release.yml only runs if the tag's tree contains it, so the orphan tree is safe until a new release.yml is added. Step 7 leave tags v0.1.0/v0.2.0/v0.2.1-rc2 and both releases untouched (assets and the MIT grant reference them); optionally edit the v0.2.0 release notes to point at `legacy`. Step 8 cleanup: delete stale remote branches (deps/tiptap-3, fix/*, test/*, spike/*, investigate/*, release/v0.2.1-rc1, chore/release-hygiene) only AFTER legacy-final is pushed, since all are contained in or superseded by main; delete local worktree-agent-*, pr20*, tier4-wave1; keep planC-capture only if the quick-capture UI is wanted. Step 9 hygiene in the new repo: no Claude-Session trailers, .claude/ and docs/plans/ scratch in .gitignore, copy docs/decisions/ as tracked ADRs.
Ruleset 20518201 has no bypass actors and forbids non-fast-forward and deletion on the default branch, so a direct `push --force` to main is rejected; moving the default branch first is the only path that never disables protection, and pushing legacy + a non-release tag first makes every later step reversible.
_rejected: (a) `git switch --orphan` inside the old checkout then force-push to main: blocked by the ruleset unless it is edited or a bypass actor is added — workable but leaves a window with no protection. (b) A brand-new repository: loses the public URL, stars, the two published releases and the MIT/AGPL provenance chain that ADR 0001 relies on. (c) Deleting old tags/releases: irrevocable loss of the MIT-era artifacts and the attestations._

### R7. Carry the release pipeline structure but shrink it: keep tag -> draft release, ci.yml called as the gate via workflow_call, SHA-pinned actions, permissions blocks, --locked/--frozen-lockfile, cargo-audit with deny=["yanked"], cargo-deny licenses/bans/sources, pnpm audit --audit-level=high, build-provenance attestations and SHA256SUMS; drop the xa11y UI gate, the cargo-vendor source tarball (unless libgit2 returns; AGPL section 6 can be met by the git archive tarball plus lockfiles — but RELEASING.md notes the npm vendoring question is unresolved), bundle-smoke, and the webkit repro. Signing/notarization: reuse the documented secret names when the owner decides to spend ~99 USD/yr; the machine has only Command Line Tools, so notarization must happen in CI (notarytool via tauri-action), not locally.
The old pipeline's gating design (same file for PR and release, draft release, attestations) is correct and cheap; the UI gate and vendoring exist for features the rewrite drops.
_rejected: Starting with a bare tauri-action workflow: the old repo's lessons (tauri-action v1 draft reuse, artifact naming, prerelease flag, tag-vs-CI race) cost several failed rc runs._

### R8. i18n: keep the tooling (i18next + react-i18next, parser gate, parity test, lint rule, pseudo-locale, typed en keys) but ship only de + en, 3-4 namespaces, and lazy-load the non-active locale.
The old setup bundled 4 locales x 15 namespaces (~400 KB of JSON) into the eager entry; the quality gates are what kept it from drifting and are worth more than the catalogs.
_rejected: Hand-rolled t() maps: loses plural rules, interpolation escaping and the CI gate that fails on empty values._

### R9. Editor decision needs the owner (see decisions): if 'Sublime-like, basic file types' dominates, use a code-editor engine (CodeMirror 6 — ASSUMED standard choice; verify current version) with Markdown highlighting and keep TipTap out; if WYSIWYG Markdown is required for notes, reuse packages/editor's NovalisEditor + WikiLink + Callout + Find + the markdownRoundTrip golden corpus, apply the four perf commits, and still use a code editor for non-Markdown files. Do not build both for the same buffer.
The brief's two editor requirements (Markdown note editing and a general text editor) are satisfied by one engine only if that engine is a text/code editor; TipTap's Markdown round trip caused the app's worst data-loss bugs.
_rejected: Monaco: heavier than the brief's 'minimal' allows (ASSUMED, not measured)._

## OWNER DECISIONS
- Editor engine: Sublime-like code/text editor (CodeMirror-class) for everything, or TipTap WYSIWYG for .md plus a code editor for other file types?
- Kanban storage location: inside the vault under .novalis/kanban/ (syncs with the cloud folder, visible to other devices) or device-local app-data? And should notes get a stable frontmatter id so card links survive renames?
- Sync scope for v1: local OneDrive/Google Drive folder only (option A), or also in-app Google Drive/OneDrive login (option B) — B needs a separate research spike on OAuth verification, scopes and quotas that this archaeology did not cover.
- Merge fix/oauth-client-id (8bb0ae5) into main before freezing it as legacy, or freeze at a00edc7?
- Keep any of: version history (app-data snapshots), in-vault trash, wikilinks + backlinks, tags, full-text search? Each is small and lifted from tested code, but each is a feature beyond 'organize, create, write, edit Markdown'.
- License for the rewrite: keep AGPL-3.0-only + COMMERCIAL-LICENSE (recommended by ADR 0001), and should the section-7 plugin and app-store exceptions be kept in LICENSE even though the rewrite has no plugins?
- Git procedure: approve moving the default branch and renaming (recommended), or prefer temporarily editing ruleset 20518201 to allow one force-push to main? Also: which required status-check names should the ruleset carry for the new CI?
- Delete the stale remote branches and local agent worktrees after legacy-final is pushed, or keep them?
- Signing/notarization spend (~99 USD/yr Apple Developer ID) now or after the first rewrite release?
- Rust toolchain: pin 1.93 (old repo, forced by specta rc.25) or float to the machine's 1.96 and verify current tauri-specta/specta versions (not verified here)?
- Should the shipped app contain KaTeX/mermaid at all (each is a multi-hundred-KB to 3 MB chunk)?
- Commit-message policy for the new repo (the old one carries claude.ai session trailers in 262 of 466 commits).

## SOURCES
- /Users/sgrundhoefer/Projects/novalis/Cargo.toml
- /Users/sgrundhoefer/Projects/novalis/Cargo.lock
- /Users/sgrundhoefer/Projects/novalis/package.json
- /Users/sgrundhoefer/Projects/novalis/pnpm-workspace.yaml
- /Users/sgrundhoefer/Projects/novalis/pnpm-lock.yaml
- /Users/sgrundhoefer/Projects/novalis/rust-toolchain.toml
- /Users/sgrundhoefer/Projects/novalis/.cargo/config.toml
- /Users/sgrundhoefer/Projects/novalis/.cargo/audit.toml
- /Users/sgrundhoefer/Projects/novalis/deny.toml
- /Users/sgrundhoefer/Projects/novalis/.gitignore
- /Users/sgrundhoefer/Projects/novalis/README.md
- /Users/sgrundhoefer/Projects/novalis/RELEASING.md
- /Users/sgrundhoefer/Projects/novalis/COMMERCIAL-LICENSE.md
- /Users/sgrundhoefer/Projects/novalis/THIRD-PARTY-NOTICES.md
- /Users/sgrundhoefer/Projects/novalis/MOBILE.md
- /Users/sgrundhoefer/Projects/novalis/docs/decisions/0001-licensing.md
- /Users/sgrundhoefer/Projects/novalis/docs/plans/PUBLIC_RELEASE_ROADMAP.md (untracked)
- /Users/sgrundhoefer/Projects/novalis/docs/plans/AUDIT_FINDINGS.md (untracked)
- /Users/sgrundhoefer/Projects/novalis/docs/plans/DEPENDENCY-BACKLOG.md (untracked)
- /Users/sgrundhoefer/Projects/novalis/docs/plans/GIT_SYNC_SPIKE.md (untracked)
- /Users/sgrundhoefer/Projects/novalis/docs/plans/TIER1_PLAN.md .. TIER4_PLAN.md (untracked)
- /Users/sgrundhoefer/Projects/novalis/.github/workflows/ci.yml
- /Users/sgrundhoefer/Projects/novalis/.github/workflows/release.yml
- /Users/sgrundhoefer/Projects/novalis/.github/workflows/e2e.yml
- /Users/sgrundhoefer/Projects/novalis/.github/dependabot.yml
- /Users/sgrundhoefer/Projects/novalis/e2e/README.md
- /Users/sgrundhoefer/Projects/novalis/e2e/package.json
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/Cargo.toml
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/lib.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/vault/fs.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/vault/frontmatter.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/vault/config.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/conflict/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/change/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/trash/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/versions/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/notes/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/index/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/index/schema.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/index/search.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/models/note.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/models/preferences.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/models/task.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/tasks/mod.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/tasks/index.rs
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/tasks/service.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/Cargo.toml
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/tauri.conf.json
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/capabilities/default.json
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/lib.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/main.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/commands.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/engine.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/oauth.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/secrets.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/settings.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/watcher.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/bg.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/autocommit.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/examples/gen_bindings.rs
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/package.json
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/package.json
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/vite.config.ts
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/src/lib/i18n.ts
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/src/locales/README.md
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/src/ipc/api.ts
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/src/ipc/bindings.ts
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/src/stores/vaultStore.ts
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/src/stores/settingsStore.ts
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/frontend/dist/index.html and dist/assets (built 2026-09-04)
- /Users/sgrundhoefer/Projects/novalis/packages/editor/package.json
- /Users/sgrundhoefer/Projects/novalis/packages/ui/package.json
- /Users/sgrundhoefer/Projects/novalis/packages/ui/src/index.ts
- /Users/sgrundhoefer/Projects/novalis/target/release/novalis-desktop and target/release/bundle/dmg/Novalis_0.2.0_aarch64.dmg (built 2026-07-13)
- git log --oneline --all -i --grep=perf --grep=slow --grep=lag --grep=freeze --grep=hang --grep=jank --grep=startup (148 lines, saved to scratchpad/perf-commits.txt)
- git show --stat d701aee 8cab4c5 e3d73f3 e9ff345 db96d61 004c8a5 d7d7406 a8b21ce 0689877 9ecb05d 0c5c3fd 70aeb81 fcfb2d9 cde9da7 0485215 0b696fc 2d9fb96 d1ef53a
- gh repo view grundhofer/novalis --json ...; gh api repos/grundhofer/novalis/rulesets and /rulesets/20518201; gh release list --json
- git branch -a; git ls-remote --heads/--tags origin; git branch --no-merged main; git log main..fix/oauth-client-id; git show -s v0.2.1-rc1
