# critique

TOOLS: No WebSearch/WebFetch were used in this run (the deferred WebFetch/WebSearch tools were available but not loaded; all time-sensitive versions/limits in the digests are therefore marked as reported, not re-verified). Used: Bash (ls, find, grep, awk, git, gh pr list, brew info, xcodebuild -version, security find-identity, xcode-select, xcrun) and file reads on the old repo, demo vault, designSprache checkout and ~/Library. Read-only; the only file written was a helper script in the session scratchpad (/private/tmp/claude-501/-Users-sgrundhoefer-Projects-novalisNeo/fc52f8b3-a73f-47eb-8cc6-a5fa40959508/scratchpad/titles.sh).

## SUMMARY
I cross-read all nine digests and re-verified the load-bearing facts locally (read-only; one helper script written to the session scratchpad). The digests agree on the big shape (Tauri + CodeMirror 6, Rust core shared with a CLI, Mode 1 cloud-folder sync first, Kanban outside notes, AGPL continuity, de+en) but contradict each other on at least ten decisions the plan cannot leave open: Kanban storage location/format/link identity (five different proposals), wikilink resolution (filename vs title, with 40 of 63 demo-vault files verified to have filename != title), whether a persistent search index exists at all (four positions), where trash goes (three), i18n tooling (three), fonts (0-60 KB vs 300-600 KB), performance budgets (perf-lessons' 150 ms window / 150 MB RSS targets sit below the stack researcher's measured Tauri hello-world floor of 166-231 ms / ~176 MB), the settings allow-list (three different lists, none yet put to the owner), the UI/UX variants deliverable (static mockups vs token-switched builds of the real app), and the git cut-over ordering (two procedures). Two brief facts are wrong: /Applications/Xcode.app 26.6 (17F113) IS installed (xcode-select merely points at the CLT), and the old checkout is on fix/oauth-client-id, not main. Nobody wrote a migration story for existing vaults: every demo note carries title/created/modified frontmatter, 63 @due( and 67 @status( tokens live in note bodies, and .novalis/config.json holds prefsVersion 1 with kanbanColumns, feature flags and appearance prefs that a deny_unknown_fields loader would reject. Naming is undecided at the level that matters: old identifier com.novalis.desktop, keychain service app.novalis, app-data at ~/Library/Application Support/com.novalis.desktop (settings.json + per-vault index dirs) — reusing it means inheriting that state and losing side-by-side installs; changing it means fresh keychain/app-data. Licensing continuity is fine (AGPL now; v0.1.0/v0.2.0 tags were MIT) but the designSprache demos are MIT and its fact sheets CC BY 4.0, so copied CSS/tokens/prose need notices nobody listed. Google Drive for desktop is not installed on this Mac (only OneDrive.app), so Mode 1 Google Drive behaviour (dot-folders, conflict-copy names, temp+rename) cannot be tested here without setup. The 'Homebrew cask as update path' is unverified (no cask named novalis exists; cask acceptance for unsigned apps is assumed to fail). No researcher gives an overall v1 effort estimate or a first-two-weeks spike order, although at least six spikes (File Provider save test, WKWebView spellcheck, CM6 typing latency, Drive dot-folder test, mockups, git cut-over) compete for the same solo developer.

## FINDINGS

### F1. Kanban storage: five incompatible proposals  [verified]
old-codebase: JSON under <vault>/.novalis/kanban/; kanban: VISIBLE <vault>/boards/<slug>/board.json + cards/<ULID>.json, whole-card LWW, losers to boards/<slug>/conflicts/; cli: <vault>/.novalis/kanban/<board-slug>.json; monorepo: cards in .novalis/kanban/<board>.json + columns in .novalis/config.json; sync: kanban/<board-id>.json one file per board with card-level LWW + tombstones and 'never a conflict copy'; perf-lessons: .novalis/kanban/<board>.json. Merge rules differ (per-card file vs per-board file; whole-card vs card-level LWW; tombstones yes/no). The old demo vault's .novalis/config.json already carries taskView.kanbanColumns with 5 columns {id,title} (backlog, todo, in-progress, review, done) that any new board store must import.
_evidence: Digests old-codebase/kanban/cli/monorepo/sync/perf-lessons; /Users/sgrundhoefer/Projects/novalis-demo-vault/.novalis/config.json (read)_

### F2. Card-to-note link identity depends on an undecided wikilink rule  [verified]
kanban links cards by wikilink target string (title or folder/title) resolved by the app's note resolver; cli and perf-lessons link by vault-relative path kept in sync by mv; old-codebase says path plus optional stable frontmatter id. Whichever is chosen must match the editor's [[wikilink]] resolution (next finding) or agents will see two different note identities in the CLI (`mv`, `relink`, `card link`).
_evidence: Digests kanban (Storage/identity), cli (--if-match, mv/relink), perf-lessons RULE 10, old-codebase Kanban design_

### F3. Wikilink resolution: filename (editor-scope) vs frontmatter title (cli/kanban); migration impact is larger than stated  [verified]
editor-scope recommends filename resolution with a one-time rename of ~36 files; cli keeps title/alias resolution from the old core; kanban assumes the old resolver. Verified on the demo vault: 40 of 63 notes have basename != frontmatter title (e.g. reading/the-quiet-machine.md -> 'The Quiet Machine', index.md -> 'Start Here', calendar/2026-08-06-design-system-sync.md -> 'Design system sync (meeting)'); 228 wikilink occurrences, 59 unique targets, 0 markdown-style links. Every existing user vault created by the old app has the same shape (old app writes title into frontmatter). A migration tool (rename files or rewrite links) is required by the filename option and nobody scoped it.
_evidence: scratchpad/titles.sh over /Users/sgrundhoefer/Projects/novalis-demo-vault (40 mismatches); grep -rhoE '\[\[[^]]+\]\]' (228); editor-scope digest_

### F4. Search/index architecture: four contradictory positions  [verified]
editor-scope: in-memory index rebuilt at open, no SQLite; cli: SQLite FTS5 index in app-data, auto-incremental before every index-backed read, search DSL facets incl. task.*; perf-lessons RULE 13: no persistent index, ripgrep-style on-demand scan, FTS5 only if the 10k-note benchmark fails; old-codebase: lift index/schema.rs (SQLite, FTS5, user_version) and the old app already keeps per-vault index dirs under ~/Library/Application Support/com.novalis.desktop/vaults/<16-hex>/. The CLI's `ls`, `links`, `tags`, `search`, `doctor` need parsed frontmatter/links for all notes per invocation if there is no persistent index; cost at 10k notes is unmeasured.
_evidence: Digests editor-scope/cli/perf-lessons/old-codebase; ls ~/Library/Application Support/com.novalis.desktop/vaults_

### F5. Dot-folder (.novalis/) under cloud sync is unverified for Google Drive, yet most proposals depend on it  [verified]
kanban could not verify Google Drive for desktop's handling of dot-folders (iCloud and Obsidian Sync reportedly skip them) and therefore chose a visible boards/ folder; cli, monorepo, perf-lessons and old-codebase all place synced state (kanban, config.json, trash) under .novalis/. perf-lessons RULE 5 additionally tells the watcher to ignore .novalis/ and the old watcher/sync manifest skipped every dot-path (kanban digest), so remote Kanban changes under .novalis/ would never be observed. Google Drive for desktop is NOT installed on this Mac (only OneDrive.app and ~/Library/CloudStorage/OneDrive-Persönlich exist), so this cannot be tested here without installing Drive and using a Google account.
_evidence: ls /Applications | grep -i drive -> OneDrive.app only; ls ~/Library/CloudStorage -> OneDrive-Persönlich; kanban and perf-lessons digests_

### F6. Trash: three different destinations  [verified]
editor-scope: macOS system Trash via trash crate 5.2.7 (no UI); cli: <vault>/.novalis/trash/ kept so it syncs; old-codebase: keep crates/novalis-core/src/trash/mod.rs (in-vault .novalis/trash, verified at trash/mod.rs:300,355). Unverified by anyone: what the trash crate does for a file inside a File Provider (CloudStorage) folder and whether OneDrive then moves it to its online recycle bin or resurrects it on the other device.
_evidence: grep '.novalis' in /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/trash/mod.rs; digests editor-scope/cli/old-codebase_

### F7. i18n tooling contradiction (and it hinges on the undecided UI framework)  [likely]
old-codebase: keep i18next + react-i18next + i18next-parser --fail-on-update gate; monorepo: i18next 26.4.2 + react-i18next + i18next-cli 1.73.1 (says i18next-parser is deprecated upstream) + eslint-plugin-i18next; perf-lessons RULE 9: no react-i18next/i18next-parser pipeline at all, a 2-key JSON per language and a 20-line t(). stack prefers vanilla TS or Solid for the shell, monorepo says React or Svelte (Svelte would make Paraglide the better fit). Versions cited (i18next-cli 1.73.1, i18next 26.4.2) come from the monorepo researcher's web fetches; I did not re-verify them.
_evidence: Digests old-codebase/monorepo/perf-lessons/stack_

### F8. Frontend framework unresolved; it gates i18n, tokens, tests and reuse  [verified]
stack: React 19 only if @novalis/ui is reused, otherwise vanilla TS/Solid; monorepo: React/Vite 'as before' or Svelte; old-codebase: reuse packages/ui + packages/editor only if TipTap is chosen (which stack/editor-scope/perf-lessons reject). No digest costs the vanilla-TS option against the old React components. This is a first-week decision.
_evidence: Digests stack/monorepo/old-codebase_

### F9. Fonts: 300-600 KB bundled (design) vs 0-60 KB budget (perf-lessons)  [verified]
design recommends self-hosting Inter variable, Newsreader, Instrument Serif, Geist Mono (~300-600 KB woff2) and never using Google Fonts CDN; perf-lessons budget says 0 bytes bundled (system font) or <= 60 KB if the style demands it; monorepo's DMG budget (<= 12 MB) has no font line. The old dist carried 1.29 MB of fonts (perf-lessons). A ~10x disagreement that also changes the mockup fidelity argument.
_evidence: Digests design (fonts), perf-lessons budget (2), monorepo performance budgets_

### F10. Performance budgets contradict each other and the measured Tauri floor  [verified]
perf-lessons: window visible <= 150 ms, idle RSS <= 150 MB, eager JS <= 250 KB gzip, <= 60 npm packages, <= 200 Cargo packages; monorepo: Tauri cold start <= 700 ms, RSS <= 180 MB, DMG <= 12 MB; stack measured on this Mac a static-HTML Tauri 2.11.5 hello world at 166-231 ms to setup() and ~176 MB summed RSS (main 85->97 MB + WebContent ~32 + GPU ~31 + Networking ~16 MB), and gpui at 105 ms / 62 MB. perf-lessons' 150 ms and 150 MB targets are below the measured baseline for Tauri; '<= 60 npm packages' is doubtful with CodeMirror 6 + @codemirror/language-data (not counted, ASSUMED). The synthesis must pick one budget set consistent with the chosen stack.
_evidence: Digests perf-lessons (budget 1, 2, 9, 10), monorepo (PERFORMANCE BUDGETS), stack (measurements)_

### F11. Settings allow-list: three lists, none consolidated for the owner's approval  [verified]
editor-scope: vault folder, language (System/de/en), appearance, font size via Cmd+/-, spellcheck toggle in Edit menu, sync account (if Mode 2); monorepo: language, style (designSprache slug), vaults[], lastVault, editor.fontSize, sync.mode ('none'|'folder'), sync.folder (<= 8 keys, deny_unknown_fields); perf-lessons: one small JSON, keys need owner yes. Conflicts: 'style' as a runtime setting contradicts choosing one UI variant; sync.mode/sync.folder are meaningless in Mode 1 (the vault path IS the cloud folder); 'vaults[]' implies multi-vault (a feature not in the brief); appearance and spellcheck are missing from monorepo's list; the old app already stores lastVault + recentVaults[] in settings.json. The brief requires asking before any setting; no digest presents one final table.
_evidence: ~/Library/Application Support/com.novalis.desktop/settings.json (lastVault, recentVaults); digests editor-scope/monorepo/perf-lessons_

### F12. UI/UX variants deliverable is defined two different ways  [verified]
design: 4 styles x 3 screens x 2 modes = 24 static HTML frames + 3 layout alternates, built with the catalog's scoping rule, before the app exists; monorepo: the same real app built against 3-5 designSprache token sets via Style Dictionary — but designSprache has no tokens/ directory today (verified absent) and monorepo itself says to hand-write tokens.css per style until then. The catalog ships exactly one colour mode per style; all dark palettes for swiss/editorial-print/warm-editorial (and the dev-noir light palette) are the design researcher's own derivations, not catalog data.
_evidence: ls /Users/sgrundhoefer/Projects/designSprache (no tokens/); styles/ has 124 files = 31 x {json, en.json, html, en.html}; digests design/monorepo_

### F13. Git cut-over: two procedures with different orderings and wrong precondition  [verified]
old-codebase: push new tree as branch 'rewrite', PATCH default_branch=rewrite, delete main, rename rewrite->main; monorepo: rename main->legacy via API first (moves default + ruleset), push orphan 'main', then `gh repo edit --default-branch main`. Both require rewriting ruleset 20518201's required_status_checks contexts (bypass_actors empty). old-codebase's precondition 'main == origin/main, clean' is wrong for the working checkout: it is on fix/oauth-client-id at 8bb0ae5 (clean, 0 changes), origin/main = a00edc7; run from a fresh clone. Live behaviour of rename+ruleset retargeting was not exercised by either researcher (both ASSUMED from docs). Open PRs verified: #99, #98, #97, #91 (dependabot) and #94 (fix/oauth-client-id); a rename retargets them to legacy. Remote refs: 23 (incl. HEAD), digests said 18; local tags include v0.2.1-rc1 which old-codebase says is not on the remote — run `git ls-remote --tags origin` before pruning.
_evidence: cd /Users/sgrundhoefer/Projects/novalis && git branch --show-current; git rev-parse HEAD origin/main; git status --porcelain | wc -l; git tag -l; git branch -r | wc -l; gh pr list --state open_

### F14. Brief and monorepo digest are wrong: full Xcode 26.6 IS installed  [verified]
/Applications/Xcode.app exists; `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -version` prints 'Xcode 26.6 Build version 17F113'; `xcode-select -p` -> /Library/Developer/CommandLineTools; notarytool resolves to the CLT copy. The stack digest is right; monorepo's 'no xcodebuild, xcstringstool, xctrace, actool' and 'swift test impossible locally' hold only on the default CLT path. The plan must state one toolchain policy (CLT default, DEVELOPER_DIR opt-in) instead of carrying the brief's wrong premise.
_evidence: ls -d /Applications/Xcode.app; xcodebuild -version with DEVELOPER_DIR; xcode-select -p; xcrun --find notarytool_

### F15. Signing: only an Apple Development identity; local notarization is possible but a Developer ID is not  [verified]
`security find-identity -v -p codesigning` lists exactly 1 valid identity: 'Apple Development: <owner> (RHAXUWNNJP)'. All digests agree a Developer ID Application cert (paid program, 99 USD/yr) is required for notarized distribution. old-codebase says notarization 'must happen in CI (notarytool via tauri-action), not locally' — contradicted by stack/monorepo, who verified notarytool 1.1.2 + stapler + codesign work under CLT. Both paths are viable once the certificate exists.
_evidence: security find-identity -v -p codesigning; digests stack/monorepo/old-codebase_

### F16. App identifier, keychain service and app-data directory: reuse or rename is undecided  [verified]
Old app: identifier com.novalis.desktop, productName 'Novalis', version 0.2.1 (tauri.conf.json:3-5); keychain service 'app.novalis' (secrets.rs:14); app-data ~/Library/Application Support/com.novalis.desktop/ with settings.json {lastVault, recentVaults[]}, vault/, vaults/<16-hex>/ (per-vault index DBs), voice/. If the new app reuses com.novalis.desktop it inherits this directory (monorepo's deny_unknown_fields Settings would reject recentVaults), shares Launch Services identity (no side-by-side legacy install during migration) and shares keychain entries; if it picks a new identifier, all of that starts fresh and the old app keeps working. monorepo proposes ~/Library/Application Support/novalis/settings.json, which under Tauri's app_config_dir implies identifier 'novalis' (non reverse-DNS; whether Tauri accepts a dot-less identifier is ASSUMED, not checked). No digest decided this and it must be fixed before the first commit of tauri.conf.json.
_evidence: /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/tauri.conf.json; apps/desktop/src-tauri/src/secrets.rs:14; ls ~/Library/Application Support/com.novalis.desktop_

### F17. No migration plan for existing vaults (frontmatter, @due/@status, .novalis/)  [verified]
Verified in the demo vault: 63/63 notes have frontmatter with title, created, modified (also status 40, rating 21, project 15, author 8, type/start/end/date 6, year 5, source 3); 63 occurrences of '@due(' and 67 of '@status(' inside note bodies plus 76 checkbox tasks; .novalis/config.json (prefsVersion 1) with taskView.kanbanColumns, taskCreation.inboxPath '_Inbox.md', fileTree, appearance, editor (autosaveMs 600, spellcheck), calendar, git, 32 feature flags, savedQueries. Old app also creates .novalis/trash/, .novalis/plugins/, .novalis/plugins-enabled.json, .novalis/state.json (sync) inside vaults and versions/ in app-data. The new plan says never rewrite frontmatter (so every old note keeps a stale 'modified:' forever), drops task parsing (so @due/@status become inert text) and moves Kanban outside notes — but nobody specifies: a `novalis migrate`/doctor step, how .novalis/config.json v1 is treated by a deny_unknown_fields loader (fail vs ignore vs new filename), whether @status tokens are stripped or converted into the new board, and whether the old columns are imported.
_evidence: awk/grep over /Users/sgrundhoefer/Projects/novalis-demo-vault; grep '.novalis' in /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src (trash/mod.rs, git/mod.rs:42-53, sync/manifest.rs:368, vault/config.rs:15); crates/novalis-core/src/versions/mod.rs:101; notes/mod.rs:162,280 (modified stamping)_

### F18. Demo vault is not a git repo and has 0 non-ASCII filenames — fixture gaps for de/en and sync tests  [verified]
/Users/sgrundhoefer/Projects/novalis-demo-vault has no .git; monorepo proposes vendoring it under fixtures/demo-vault (manual sync). It contains zero .md filenames with non-ASCII characters, so NFC/NFD normalisation (sync digest), umlaut wikilinks, case-only renames and the German OneDrive folder name ('OneDrive-Persönlich', verified) are not covered by the only fixture. No digest proposes a synthetic fixture with umlauts, NFD-encoded names, conflict-copy names, dataless placeholders and 10k/50k notes, although perf-lessons and monorepo both ask for a large-vault benchmark.
_evidence: git -C /Users/sgrundhoefer/Projects/novalis-demo-vault rev-parse (not a repo); find . -name '*.md' | grep -P '[^\x00-\x7F]' -> 0; ls ~/Library/CloudStorage_

### F19. Licensing continuity is fine, but third-party notices are unlisted  [verified]
Current LICENSE is AGPL ('Novalis Copyright (C) 2026 ...'); `git show v0.2.0:LICENSE` and v0.1.0 are MIT (so those releases stay MIT-licensed artifacts; old-codebase's 'MIT grant reference' confirmed). designSprache: styles/*.html demos and build.py are MIT (LICENSE), fact sheets styles/*.json are CC BY 4.0 (LICENSE-CONTENT.md) — copying demo CSS/tokens into the AGPL app needs the MIT notice; quoting fact-sheet prose (palette rationale, tips) in docs/mockups needs CC BY attribution. Bundled fonts under SIL OFL 1.1 require shipping the OFL text (design verified OFL, did not list the obligation). None of the digests carries a THIRD-PARTY-NOTICES list or decides whether the section-7 plugin/app-store exceptions survive in a plugin-less app (old-codebase flags the latter).
_evidence: git show v0.2.0:LICENSE | head -2; head LICENSE; /Users/sgrundhoefer/Projects/designSprache/LICENSE and LICENSE-CONTENT.md_

### F20. Homebrew cask as the update path is unverified  [likely]
monorepo proposes GitHub Releases + `brew upgrade --cask novalis` instead of an updater. `brew info --cask novalis` -> 'No Cask with this name exists' (name free, but a cask must be created and maintained, either in homebrew-cask or a personal tap). Whether homebrew-cask accepts an unsigned/un-notarized app is ASSUMED to be a blocker (policy not fetched). This ties the update story to the Developer ID decision.
_evidence: brew info --cask novalis; monorepo digest AUTO-UPDATE POLICY_

### F21. Minimum macOS version is undecided; old app declared 11.0  [verified]
Old tauri.conf.json bundle.macOS.minimumSystemVersion = '11.0', bundle.targets = 'all'. stack lists min macOS as open (it fixes the WebKit/Safari feature level under Tauri; CM6 changelog mentions Safari 26 fixes); sync's SF_DATALESS flag and File Provider CloudStorage paths require macOS 12.1+ for the OneDrive File Provider (ASSUMED). No digest proposes a number; it affects CSS features usable in the mockups too.
_evidence: grep minimumSystemVersion /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/tauri.conf.json; stack digest OPEN_

### F22. Autosave vs explicit save and File Provider write safety are unresolved and coupled  [verified]
editor-scope: debounced atomic autosave for all file types; perf-lessons: owner decides autosave vs Sublime-style dirty marker + Cmd-S; sync and perf-lessons both flag as UNVERIFIED whether temp+rename saves create conflict copies, drop xattrs or confuse OneDrive/Google Drive File Provider domains. A real OneDrive folder exists on this machine (~/Library/CloudStorage/OneDrive-Persönlich) so this can be tested in week one; Google Drive cannot be tested here today.
_evidence: Digests editor-scope/perf-lessons RULE 4/sync Mode 1 caveats; ls ~/Library/CloudStorage_

### F23. External-change handling has no single owner spec (editor, watcher, CLI, sync)  [verified]
cli needs the desktop to reload clean buffers and banner dirty ones so `--if-match <sha256>` holds end-to-end; editor-scope lists 'conflict-copy on concurrent external change' only as SHOULD; perf-lessons RULE 4 says refuse the save and surface a conflict; sync (Mode 2) needs self-write suppression shared with CLI writes; old-codebase reports the old debounce window silently discarded typing. Four partial rules, no state machine, no UI text (de/en) for the banner.
_evidence: Digests cli (OPEN last item), editor-scope (SHOULD list), perf-lessons RULE 4/5, sync OPEN last item, old-codebase perf history_

### F24. Keyboard and layout contradictions between design and editor-scope  [verified]
design's default layout proposal is 'no tabs, quick-open via ⌘P' and a dev-noir '⌘K badge' for the command palette; editor-scope recommends tabs (Shift-Cmd-[ / ], Cmd-1..9), Cmd-Shift-P for the palette, Cmd-K for Markdown link, and flags Cmd-P (goto file) vs macOS Print. Kanban placement (toggled pane vs separate window) is raised only by design.
_evidence: Digests design (Layout, Window chrome), editor-scope (Keymap)_

### F25. Sync Mode 2 reuse contradicts the DROP list  [verified]
sync recommends lifting the pure 3-way planner novalis_core::sync::manifest (plan/next_base) and oauth.rs; old-codebase's DROP list says 'sync/* + iroh P2P' entirely (keeps oauth.rs/secrets.rs). Needs one line: drop iroh transport and state.json, keep the planner as reference. Note the old manifest also treated .novalis/ as internal and non-syncable (sync/manifest.rs:367-384), the opposite of what a Mode 2 Kanban sync needs.
_evidence: Digests sync/old-codebase; /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/sync/manifest.rs:367-384_

### F26. Kanban conflict behaviour differs by sync mode and the plan must say so  [verified]
kanban: vendor conflict copies ('<id>-<COMPUTER>.json', '<id> (1).json', Google name undocumented) detected generically and resolved by whole-card LWW; sync (Mode 2 engine): 'never produce a conflict copy for Kanban files', card-level LWW with tombstones. In Mode 1 the app does not control conflict-copy creation at all, so the kanban rule is the only one that can apply; if Mode 2 is ever built both rules coexist. Also unverified by both: Google Drive for desktop's conflict-copy naming.
_evidence: Digests kanban (Conflict story) and sync (Kanban data)_

### F27. Tasks/@due fate and Kanban migration undecided  [verified]
editor-scope drops tasks/@due parsing; cli asks whether inline tasks survive; the old Kanban derived cards from @status(...) written INTO note lines (old-codebase, tasks/index.rs:506-540). The demo vault has 63 '@due(' and 67 '@status(' tokens. Options nobody costed: leave tokens as inert text, strip them with a migration, or convert @status cards into the new external board once.
_evidence: grep -rhoE '@(due|status)\(' over the demo vault; digests editor-scope/cli/old-codebase_

### F28. No overall effort estimate or week-1/2 spike order  [verified]
Only sync gives estimates (Mode 1 ~1 week; Mode 2 ~2 months incl. vendor verification). Competing first-two-week items with no sequencing: (1) File Provider temp+rename test on the real OneDrive folder; (2) WKWebView spellcheck underline spike (tauri#7705 open since 2023, editor-scope); (3) Tauri+CM6 typing-latency / 50 MB spike that stack makes the fallback trigger (2 weeks); (4) Google Drive dot-folder + conflict-name test (needs Drive installed); (5) 24+3 mockup frames (design); (6) git cut-over + ruleset edit; (7) settings/ADR/CLAUDE.md scaffolding. A solo developer cannot do all seven in two weeks.
_evidence: Digests sync (Effort plan), stack (fallback condition), editor-scope (spellcheck spike), design (variant set), monorepo (branch procedure)_

### F29. Rust toolchain pin is a floor, not a ceiling; current specta/tauri-specta versions unverified  [verified]
rust-toolchain.toml pins channel 1.93.0 because specta =2.0.0-rc.25 uses core::fmt::from_fn 'stabilized after 1.91' — 1.96 satisfies that; the pin exists for reproducibility. monorepo proposes 1.96.0; stack cites Tauri 2.11.5 from the web; old-codebase and stack say current tauri-specta/specta versions were NOT verified. tauri-action v1.0.0 comes from the old release.yml.
_evidence: /Users/sgrundhoefer/Projects/novalis/rust-toolchain.toml; apps/desktop/src-tauri/Cargo.toml:20-24; digests_

### F30. Web-fetched versions/limits in the digests were not re-verified by me  [assumed]
I had no web tools in this run. Claims taken on trust from researchers who said they used WebFetch/WebSearch: Tauri 2.11.5, @codemirror/view 6.43.11, i18next-cli 1.73.1, i18next 26.4.2, rmcp 3.2.0, clap 4.6, ulid 3.0.0, uuid 1.26.0, diffy 0.5.2, pulldown-cmark 0.13.4, notify 8.2.0, trash 5.2.7, objc2-app-kit 0.3.2, Sparkle 2.9.6, Style Dictionary 5.5.2, gpui 0.2.2/gpui-component 0.6.0, Drive v3 discovery rev 20260901, Graph delta/410 semantics, CASA cost range, Microsoft 90-day refresh tokens, Google client_secret policy. The synthesis should mark these 'as reported on 2026-09-05' and re-check before pinning lockfiles.
_evidence: ASSUMED (digest text only)_

### F31. Multi-vault, version history and backlinks appear in proposals without an owner yes  [verified]
monorepo's settings list has vaults[] (multi-vault); old app kept recentVaults[]; old-codebase asks about version history/in-vault trash/wikilinks+backlinks/tags/full-text search; kanban proposes 'cards linking here' in a backlinks panel; cli proposes relink, meta, doctor --fix, init, help --json, skill, mcp. Each is beyond 'organize, create, write, edit Markdown + Kanban + sync' and the brief demands asking first. No digest merges these into one approval list.
_evidence: Digests monorepo/old-codebase/kanban/cli OPEN sections_

### F32. German i18n gaps not covered by any digest  [likely]
Missing: localisation of Tauri native menus (menu items are built in Rust and must draw from the same catalog; only predefined items like Quit/Hide get system names), de/en strings for the conflict/external-change banners and Kanban conflict notice, locale-aware date rendering for frontmatter created/modified (ISO in files, de/en in UI), NFC/NFD for umlaut filenames, and how the mockups (German first per design) get their English pass. CLI English-only is consistent across cli and monorepo.
_evidence: Digests design (mockup language), monorepo (I18N SETUP), cli; gap analysis_

### F33. Cloud-folder detection rule for Mode 1 is unspecified  [likely]
sync and perf-lessons specify SF_DATALESS detection and placeholder handling, but nobody defines how the app decides a vault 'is in a cloud folder' (path under ~/Library/CloudStorage/<Provider>-<account> is the documented File Provider location and exists here for OneDrive; legacy ~/OneDrive locations and symlinks are not covered) nor the hint UX (sync OPEN). The CLI's `sync status` (cli) depends on the same rule.
_evidence: ls ~/Library/CloudStorage; digests sync/perf-lessons/cli_

## RECOMMENDATIONS

### R1. Add a 'Contradictions resolved' table to the plan that fixes, with one line each: Kanban location+format+merge rule, card link identity, wikilink resolution, index/no-index, trash destination, i18n tooling, UI framework, fonts, performance budget set, settings allow-list, variants deliverable, git procedure. Every row cites which researcher's option was taken and why the others were rejected (Rule 3: pick, don't blend).
Ten cross-researcher contradictions cannot be left to implementation time; several (identifier, wikilink rule, Kanban path) are irreversible once a vault is written.
_rejected: Leaving them as owner-decision bullets per researcher duplicates questions and hides the dependencies between them._

### R2. Adopt the visible per-board directory (kanban digest) OR the .novalis/ location only after a one-day test on the real OneDrive folder here and a Google Drive for desktop install; until tested, treat any dot-folder proposal as unverified. If .novalis/ wins, delete perf-lessons RULE 5's 'ignore .novalis/' watcher rule for that subtree.
Three researchers assume dot-folders sync and are watched; one verified that the old watcher and sync manifest skipped them and that Google's policy is undocumented.
_rejected: Deciding by preference without the test._

### R3. Decide the note-identity chain as one unit: wikilink resolution rule -> card link field -> CLI mv/relink behaviour -> migration tool. Recommend filename-based resolution plus a `novalis migrate` (or `doctor --fix`) command that renames the 40 mismatched demo files (and any user vault) or rewrites links, run once with --dry-run output.
Filename resolution is what agents and Obsidian expect; title resolution needs an index the perf researcher wants to avoid. Either way the migration is real work that no digest scoped.
_rejected: Title-based resolution keeps old vaults intact but forces a persistent index and ambiguous identities for the CLI._

### R4. Write a 'Migration from old novalis' section: identifier decision (recommend a NEW bundle identifier and keychain service so the legacy app stays installable side by side), treatment of .novalis/config.json v1 (ignore unknown keys and import taskView.kanbanColumns into the first board), fate of @due/@status tokens (recommend: leave as text, no rewrite), stale created/modified frontmatter (never touched), old app-data dir left untouched, and a de/en release note for legacy users.
Every existing vault was written by the old app; the brief says 'replaces forever' but users' data outlives the app.
_rejected: Reusing com.novalis.desktop and app.novalis: inherits settings.json/recentVaults and index dirs and blocks side-by-side use during migration._

### R5. Fix the toolchain policy in CLAUDE.md: CLT is the default path; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer is allowed for `swift test`/Metal/xcstrings when needed. Correct the brief's 'no Xcode' premise in the plan.
Xcode 26.6 (17F113) is verified installed; two digests planned around its absence.
_rejected: Keeping the wrong premise leads to CI-only tests and gpui being wrongly ruled out._

### R6. Reconcile performance budgets against the stack researcher's measured floor: window visible <= 300 ms and idle RSS <= 200 MB summed (or main-process-only <= 100 MB) for Tauri; keep perf-lessons' eager-JS (<= 250 KB gzip), IPC (<= 3 at boot), keystroke (<= 8-16 ms p95) and save (1 fsync, 0 re-reads) rules; drop the '<= 60 npm packages' number until counted with CM6 installed; set one font budget (owner decision, 0/60 KB vs 300-600 KB).
Unreachable budgets fail CI from the first commit and get disabled.
_rejected: Keeping both budget sets._

### R7. Sequence the first two weeks explicitly: week 1 = git cut-over (from a fresh clone, monorepo's rename-first order, then rewrite ruleset checks), scaffold + CLAUDE.md + ADR-0001 (license) + ADR-0002 (no update check) + ADR-0003 (identifier), OneDrive File Provider save test, WKWebView spellcheck spike; week 2 = Tauri+CM6 typing-latency spike with a generated 10k-note vault, mockups (reduce to 2 finalist styles x 2 screens x 2 modes), Google Drive install + dot-folder test. Push Mode 2 sync, MCP, relink, doctor --fix to post-v1 ADRs.
Seven spikes compete for one developer; the stack digest's fallback decision needs the CM6 spike result early.
_rejected: Building all 27 mockup frames first (design) delays the stack-risk spike._

### R8. Add a single 'External change' spec shared by editor, watcher, CLI and sync: (mtime,size,hash) captured at load; save refuses on mismatch and shows a de/en banner with 'reload / keep mine / save as conflict copy'; clean buffers reload silently; self-writes matched by content hash; CLI `--if-match` maps to the same check.
Four digests each own a fragment; the old app's silent-discard bug came from exactly this gap.
_rejected: Per-component rules._

### R9. Add a THIRD-PARTY-NOTICES policy to the plan: MIT notice for any CSS/tokens lifted from designSprache styles/*.html, CC BY 4.0 attribution for fact-sheet prose, OFL texts for bundled fonts, cargo-deny + a JS licence check in CI; decide whether ADR-0001's section-7 exceptions are kept verbatim.
AGPL continuity is agreed but the inbound licences of copied design assets were never listed.
_rejected: None._

### R10. Replace the demo vault as sole fixture with: vendored demo vault + a generated fixture containing umlaut/NFD filenames, case-only twins, conflict-copy names for OneDrive/Drive/Dropbox, dataless placeholders (documented manual checklist on the real OneDrive folder), and 10k/50k synthetic notes for benchmarks.
The demo vault has 0 non-ASCII filenames and is not a git repo; de/en and sync edge cases are untested by it.
_rejected: Relying on the demo vault only._

### R11. Present the owner ONE consolidated approval list (settings keys, features beyond the brief, dependencies with network calls) merged from all nine digests, each item with a default 'no' and the researcher who proposed it, instead of nine separate OPEN lists.
The brief demands asking before any setting/feature; scattered lists guarantee something ships unasked.
_rejected: Per-researcher question lists._

## OWNER DECISIONS
- New bundle identifier + keychain service (e.g. new reverse-DNS id, fresh app-data dir) so old and new novalis can coexist during migration — or reuse com.novalis.desktop / app.novalis and inherit ~/Library/Application Support/com.novalis.desktop?
- Wikilink resolution by filename (with a one-time migration renaming or relinking 40/63 demo files and equivalent user vaults) or by frontmatter title as before (requires a persistent index)?
- Kanban store: visible <vault>/boards/<slug>/{board.json,cards/<ULID>.json} vs hidden <vault>/.novalis/kanban/… vs device-local app-data; and card links by wikilink target vs vault-relative path.
- Persistent search index (SQLite FTS5 in app-data, shared by desktop and CLI) vs in-memory index vs on-demand grep-only search — the CLI's command surface depends on it.
- Trash: macOS system Trash (per machine, zero UI) vs in-vault .novalis/trash (synced, needs UI).
- Fonts: bundle OFL webfonts (300-600 KB) for catalog fidelity or system fonts (0 bytes)?
- Final settings allow-list (proposed union: vault/lastVault, language, appearance, editor.fontSize, spellcheck; 'style', 'vaults[]', 'sync.*' need explicit yes/no).
- UI/UX variants deliverable: static mockup frames first (how many styles/screens/modes), token-switched builds of the real app, or both in sequence?
- Frontend shell: React 19 (reuse) vs vanilla TS/Solid/Svelte (lighter) — decides i18n tooling.
- Git cut-over: monorepo's rename-main-to-legacy-first order (recommended) vs old-codebase's push-rewrite-then-switch order; merge PR #94 into legacy first or close it; prune the 4 dependabot PRs and stale branches.
- First rewrite version tag (v2.0.0-alpha.1 proposed) and whether the first release takes the 'Latest' badge from v0.2.0.
- Apple Developer Program (99 USD/yr) now or ship unsigned first; consequently whether a Homebrew cask/tap is the update path.
- Toolchain policy: allow DEVELOPER_DIR=/Applications/Xcode.app for tests/tools, or strictly CLT?
- Autosave (debounce value) vs explicit Cmd-S with save-on-blur/quit.
- Fate of @due/@status tokens and inline tasks in existing notes: inert text, stripped by migration, or imported into the new board once.
- Mode 2 (in-app Google Drive/OneDrive login) in v1 at all, and if yes, shipping OAuth client credentials in the binary.
- Tabs vs no tabs; Cmd-P = goto file vs Print; Cmd-K = palette vs link; Kanban as pane vs window.
- Minimum macOS version (old app: 11.0; File Provider CloudStorage needs newer) and arm64-only vs universal.
- Multi-vault support (vaults[]/recentVaults) in v1 or single vault only.
- Features beyond the brief proposed by researchers, each needing yes/no: backlinks panel, version history, relink command, meta command, doctor --fix, init, help --json/skill, MCP server, read-only Markdown preview (Cmd-E), soft-delete tombstones, board.json unions.

## SOURCES
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/tauri.conf.json (identifier com.novalis.desktop, productName Novalis, version 0.2.1, minimumSystemVersion 11.0, targets all)
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/secrets.rs:14 (KEYRING_SERVICE = "app.novalis")
- /Users/sgrundhoefer/Projects/novalis/apps/desktop/src-tauri/src/settings.rs:52, commands.rs:135,196, lib.rs:353 (app_config_dir/app_data_dir usage)
- /Users/sgrundhoefer/Projects/novalis/crates/novalis-core/src/{trash/mod.rs:300,355; git/mod.rs:42-53; sync/manifest.rs:367-384; vault/config.rs:15; versions/mod.rs:101; notes/mod.rs:87,162,178,280}
- /Users/sgrundhoefer/Projects/novalis/rust-toolchain.toml; Cargo.toml:34; apps/desktop/src-tauri/Cargo.toml:17-30
- /Users/sgrundhoefer/Projects/novalis: git branch --show-current (fix/oauth-client-id), git rev-parse HEAD (8bb0ae5) origin/main (a00edc7), git status --porcelain (clean), git tag -l (v0.1.0 v0.2.0 v0.2.1-rc1 v0.2.1-rc2), git branch -r | wc -l (23), git show v0.2.0:LICENSE (MIT), LICENSE (AGPL header), README.md:115, SECURITY.md:9
- gh pr list --state open on grundhofer/novalis (#99 #98 #97 #94 #91)
- /Users/sgrundhoefer/Projects/novalis-demo-vault: find/awk/grep counts (63 notes, 63 title/created/modified, 63 @due(, 67 @status(, 76 checkboxes, 228 wikilinks, 59 unique targets, 40 filename!=title, 0 non-ASCII filenames), .novalis/config.json, no .git
- ~/Library/Application Support/com.novalis.desktop/{settings.json,vault,vaults/<hex>,voice}
- /Users/sgrundhoefer/Projects/designSprache/{LICENSE (MIT for styles/*.html, build.py, tools/), LICENSE-CONTENT.md (CC BY 4.0 for styles/*.json), styles/ (124 files), no tokens/}
- ls -d /Applications/Xcode.app; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -version (Xcode 26.6 Build 17F113); xcode-select -p (CLT); xcrun --find notarytool
- security find-identity -v -p codesigning (1 valid: Apple Development)
- ls ~/Library/CloudStorage (OneDrive-Persönlich only); ls /Applications | grep -i drive (OneDrive.app, no Google Drive)
- brew info --cask novalis (no such cask)
- The nine digests supplied in the task prompt (old-codebase, design, stack, sync, kanban, cli, editor-scope, monorepo, perf-lessons)
