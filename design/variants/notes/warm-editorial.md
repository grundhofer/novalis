# warm-editorial

fixed round applied: True

## Notes
FIXES APPLIED (one per violation, everything else byte-identical to the previous stylesheet apart from the header comment):

1. Fit (l2/l3): .line.blank 18 -> 16px (-16), .line.fm line-height 18 -> 16px (-8; the violation's 17px landed at 15.1px slack because the h2 line is 35.9px, not 36), .editor margin-bottom 10 -> 6px (-4), and the sheet's bottom padding raised 24 -> 32px (+8) as requested. Measured in headless Chrome with the Google Fonts loaded: l2 and l3 last-line bottom 713.9 vs sheet inner bottom 733.0 = 19.1px slack (was 0); l1 = 43px; status bar 772-800 in every frame. Reading text stays 16px/26px (1.625), only blank and frontmatter lines were tightened.
2. .tab colour ink-3 -> ink-2, hover -> ink; .tab.active unchanged (ink + accent bar). Measured rgb(92,83,72) light / rgb(201,191,176) dark on the inactive tabs.
3. .line.task.done .txt -> ink-2 (measured), the filled green checkbox carries 'done'.
4. .palette-foot -> ink-2 and set as a spec-conformant micro-label (10px / 500 / .14em / uppercase); .palette-foot .kbd gets text-transform: none and restates letter-spacing .06em so the glyph chips are untouched. Verified in the l3 dump (foot 10px uppercase ink-2; each .kbd tt=none).
5. Added --btn-shadow-hover to both token blocks and referenced it from .btn.primary:hover; the hard-coded rgba(52,40,26,.6) is gone (grep: no colour literal outside the two token blocks). I chose the 'identical geometry, deeper alpha' option (8/20/-10 at .70 light / .75 dark) rather than the fix's 12/26/-12 values, so box-shadow may stay in the .btn transition list: the ghost button's underline is a box-shadow colour change (rule -> accent) and would otherwise snap. Measured resting shadows: rgba(52,40,26,.55) light, rgba(10,6,2,.6) dark. Hover itself is not measurable in a static screenshot (unverified visually, verified by token resolution).
6. l5 split: .card-note-name now nowrap + ellipsis with min-width:0 / flex:1 1 auto (required for ellipsis inside the flex chip); .split-board .columns gap 16 -> 10px. I first applied the suggested .split-board { flex: 1.5 } and MEASURED it: the note pane fell to 415px, p7 wrapped to 4 lines plus five other lines to 2, and the 26-line note overran its sheet by 29px (clipped) - a regression, so it was replaced by flex 1.25 (board 575 / note 461px, note slack 40px, no extra wraps). At that width column content is 109px; a single-row .card-meta needs ~130-150px, so one-row metas are unreachable in l5 without starving the note. Instead of the previous mixed wrap (3 of 9 cards on two rows) the split metas are now a deliberate, uniform two-row stack (state label above, italic date below, 3px gap) - every card measures h=36. Split column padding 12 -> 10px (hover/selected negative margins matched), split card-title 14.5 -> 14px, split chip 13.5 -> 13px: the measurement had shown 'Wochenrückblick' (111px) running 8px into the padding at 103px content and the 'Offline-First UX' chip truncated to two characters; now the title fits (no overflow flagged), 'Offline-First UX' renders whole, and only 'Atlas Rendering Spec' is ellipsized. Columns end far above their sheets (max card bottom 524 vs 788).
7. Padding deviations declared below; main sheet bottom padding raised to 32px.
8. .hint-dot 12 -> 14px (measured 14x2), matching .state-mark and .status-dot.

VERIFICATION: validate_css.py warm-editorial -> 0 problems (all selectors scoped, dark token block present, no url/import/backdrop-filter/blur). Full-frame measurement + screenshots in headless Chrome 152 at 1280x800 with the same Google Fonts link as the comparison page: s1-l1-light, s1-l2 light+dark, s1-l3-light, s2-l4-light, s2-l5 light+dark all inspected by eye; l4 dark / l1 dark / l3 dark were not screenshotted after the final edits (the killed first run never reached them) - their geometry is identical to the light frames and the only theme differences are token colours. Not verified: hover states, the comparison page's scaled render, and behaviour under font fallback (Georgia) - a fallback would change every wrap count and the fit budget with it.

OBSERVATION LEFT UNCHANGED (out of scope): inside .split-note, `.split-note .line` (line-height 23px) outranks `.line.h1 { line-height: 1 }` and `.line.h2` at equal specificity by source order, so the split headings inherit the 23px line-height; they still render cleanly (measured 35/33px boxes via baseline alignment) so I did not touch them.

WORKING FILES (scratchpad only, not deliverables; the author's css/warm-editorial.css was not modified): /private/tmp/claude-501/-Users-sgrundhoefer-Projects-novalisNeo/fc52f8b3-a73f-47eb-8cc6-a5fa40959508/scratchpad/mockups/test/we-fix/warm-editorial.css (this stylesheet), measure.sh / measure_par.sh (build + measurement harness), shots/*.png, dumps/*.

## Deviations
1. Sheet inner padding: main editor 40px 48px 32px - the bottom is below the spec's 40-56px. Reason: the 800px frame budget (titlebar 32 + tabs 34 + sheet chrome + 26-line document + status bar) leaves no room for 40px at the bottom while keeping >=16px of slack under the last line; 32px was the reviewer's suggested compromise and measures 19.1px slack in l2/l3.
2. Split-note sheet (l5) padding 20px 26px 16px and its reduced type (15px/23px, blank 12px, h1 30px): far below the 40-56px rule, because the note pane is 461px wide and shares the 800px height with tabs and status bar; with the spec padding the 26 lines would not fit. Measured slack there is 40px.
3. Blank lines 16px and frontmatter lines 16px: the spec's 1.6-1.75 line-height applies to reading text (kept at 16/26 = 1.625); blank and frontmatter lines are structural/meta lines tightened purely for the fit budget.
4. Split-board ratio flex 1.25 instead of the base 1.2 or the suggested 1.5: 1.5 was measured to break the note pane (26 lines overrun by 29px); 1.25 is the widest ratio that leaves the note its slack while letting the longest single title word and the 'Offline-First UX' chip fit in the columns.
5. Split-board card-meta as a deliberate two-row stack (state above, date below) rather than one row: a one-row meta needs ~130-150px per column, unreachable in the split without starving the note; the uniform stack replaces the earlier inconsistent mixed wrapping.
6. Split-board card-title 14px and card-note chip 13px (main board keeps 16px / 13.5px): below the spec's 16-21px reading-text floor, as the previous 14.5px already was; needed so 'Wochenrückblick' clears the 109px column content and the chip renders whole.
7. Primary button hover shadow: same geometry as rest, only alpha deepened (.55 -> .70 light, .60 -> .75 dark), instead of the fix's 12/26/-12 values, to keep motion to colour/opacity and box-shadow in the .btn transition for the ghost underline colour change.
8. .card-note-name in the split is ellipsized ('Atlas Rendering Spec' -> 'Atlas Renderin...'): sanctioned by the reviewer's fix; the main board (l4) shows the full names.

## Critic summary
Warm Editorial passes every hard gate. Scoping: the official validate_css.py reports 0 problems; all 145 selector groups start with .style-warm-editorial, the only at-rule is @media (prefers-reduced-motion) with prefixed selectors inside, and there is no url()/@import/backdrop-filter/filter/gradient/@keyframes (the single 'gradient' hit is the header comment). Spec: both palettes match the spec hex-for-hex (light = catalog demo tokens, dark = the spec's derived warm ladder), sheet shadow is the demo's exact 3-layer warm stack, dark shadows are rgba(10,6,2,...) not #000, no neutral grey anywhere, radius 14 on sheets/10 on primary button/0 on fields and rules, hairlines in rule/rule-soft, Newsreader/Instrument Serif/Inter/ui-monospace stacks as specified, micro-labels 9.5-10px at .14-.16em weight 500, status coded accent/green/ochre + word + 14x2 dash (.state-mark, .status-dot), native title bar on paper, sidebar boxless, editor = sheet with 1px rule-soft border, textures off, 200ms ease on colour/opacity/border. Coverage: .card.selected, .badge-cloud/.badge-conflict, .board-item, .line.cursor, .tab.active, all .palette-* and .split-* classes, and a complete dark token block are present. Fit: the author's saved css/warm-editorial.css is byte-identical to the reviewed text, so their 1280x800 screenshots are valid evidence; I inspected s1-l2 light+dark, s2-l4 light+dark, s2-l5 light, s1-l3 light, plus my own s1-l1 dark render: every frame keeps the status bar inside 800px, 14 tree rows, 26 lines, 3 tabs and 4 columns/9 cards are all visible, columns are ~240px wide in l4. Not visually verified: s1-l3 dark and s2-l5 dark (colour-only twins of frames that fit). Remaining problems are second-order and listed below: a zero-pixel vertical budget on the tabbed editor layouts (fits by arithmetic exactly 32+34+2+64+630+10+28=800 and by screenshot with no slack), three places where ink-3 (3.7-3.9:1 in light) is used for text that is read rather than glanced at, one un-tokenized hover shadow, a cramped l5 board, an undeclared padding deviation, and a 12px hint dash in a 14px dash system. Minor: the file is 740 lines against the ~350-700 guidance (validator does not check this).

## Violations found by the critic
- [medium/fit] .style-warm-editorial .editor (line 299) / .line.blank (line 319) — layouts l2 and l3: On the tabbed editor layouts the 26-line doc ends exactly at the sheet's bottom padding with 0px slack: titlebar 32 + tabs 34 + sheet border 2 + padding 40+24 + doc 630 (4 fm x18, h1 52, 8 blank x18, h2 36, 3 li x26, quote 26, 2 fence x22, code 22, 2 task x26, p7 wrapping to 2 lines, p17 and tags 26 each) + margin 10 + statusbar 28 = 800. The author's s1-l2 screenshot confirms it: the '#pkm #ux' line box ends at ~737 and the sheet edge sits at 762 = padding+border only. Any extra wrap (fallback font, opsz change, 1px rounding in the comparison page's scaled render) clips the last line inside the overflow-hidden sheet; the status bar is safe but the 26-line requirement is not. → Buy real slack without touching the 40px top or 48px side padding: set .line.blank to height/line-height 16px (-16px) or .line.fm line-height 17px (-4px), or drop .editor margin-bottom to 6px; aim for >=16px of slack in l2/l3.
- [medium/contrast] .style-warm-editorial .tab (line 276): Inactive tab titles ('Atlas Overview', '2026-08-05') are 14.5px Newsreader in ink-3 = 3.71:1 on paper (light). That fails AA for normal text and the spec's own rule that ink-3 is for casual metadata only while anything read or interactive runs in ink-2 (7.05:1). Visible in the screenshot as noticeably faint running heads. → .style-warm-editorial .tab { color: var(--ink-2); } and move the hover step to var(--ink); keep .tab.active as ink + accent bar. (Dark ink-3 is 5.45:1 and is fine either way.)
- [medium/contrast] .style-warm-editorial .line.task.done .txt (line 395): Completed-task body text ('Spaltennamen mit dem Team abstimmen') is 16px reading text set in ink-3 = 3.91:1 on the sheet (light). It is content, not metadata, so it breaks the spec's ink-3 budget and AA. → Use var(--ink-2) (7.42:1 on sheet); the filled green checkbox already carries the 'done' signal.
- [low/contrast] .style-warm-editorial .palette-foot (line 721): The palette footer hints ('öffnen', 'in neuem Tab', 'Befehle') are 11px Inter in ink-3 = 3.91:1 on the sheet; they are labels that are read, which the spec puts in ink-2. Also this is Inter used as plain text rather than as a tracked micro-label, which the spec reserves Inter for. → color: var(--ink-2) on .palette-foot; optionally set the hint words as 10px caps .14em micro-labels so the Inter use is spec-conformant, leaving .kbd as is.
- [low/spec] .style-warm-editorial .btn.primary:hover (line 495) and .btn transition (line 485): The only colour literal outside the two token blocks: the hover drop shadow is hard-coded rgba(52,40,26,.6), so in dark mode the primary button's shadow switches tint from rgba(10,6,2) to a light-mode brown on hover. Additionally box-shadow is in the transition list and the hover changes shadow geometry (8/20/-10 -> 12/26/-12), an implied lift outside the 'colour/opacity only' motion rule (the spec's sanctioned lift is a 1px translate, which the stylesheet correctly does not use). → Add --btn-shadow-hover to both token blocks (light: 0 12px 26px -12px rgba(52,40,26,.6); dark: 0 12px 26px -12px rgba(10,6,2,.7)) and reference it; either keep the hover shadow geometry identical and only deepen alpha, or remove box-shadow from the .btn transition.
- [low/fit] .style-warm-editorial .split-board .column (line 611) / .card-note in layout l5: In the l5 split the base flex ratio (1.2:1) leaves four ~125px columns (~100px content after 12px padding). The card-note chip 'Atlas Rendering Spec' wraps to three lines beside its glyph and every .card-meta wraps to two rows (state word above, date below) — visible in the author's s2-l5 screenshot. It fits inside 1280x800 but reads as broken rather than 'luftig'. → Add .style-warm-editorial .split-board .card-note-name { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; } and tighten .split-board .columns { gap: 10px; }; or widen the board with .style-warm-editorial .split-board { flex: 1.5; } (a base override the rules allow).
- [low/spec] .style-warm-editorial .editor padding (line 299: 40px 48px 24px) and .split-note .editor (line 619: 20px 26px 16px): The spec's hard parameter is 40-56px inner padding on the sheet. The main editor's bottom padding (24px) and the whole split-note sheet (20/26/16) fall below it. Both are defensible fit trade-offs (the l2 budget above forces the first) but they are conscious departures from a hard parameter and must appear in the 'deviations' field, not only in the header comment ('generous inner padding'). → Declare both in deviations with the fit reason; if the l2 slack fix above is applied, raise the main sheet's bottom padding to 32px so it sits nearer the range.
- [low/spec] .style-warm-editorial .hint-dot (line 260): The sidebar hint dash is 12x2px while the status system everywhere else (.state-mark, .status-dot) is the spec's 14x2px dash; the mismatch is small but it is the one place the dash grammar is not consistent. → width: 14px.
