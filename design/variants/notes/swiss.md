# swiss

fixed round applied: True

## Notes
All seven violations applied; everything else is byte-identical to the previous stylesheet (456 lines). Verification was done with the reviewer's own headless-Chrome harness, copied to /private/tmp/claude-501/-Users-sgrundhoefer-Projects-novalisNeo/fc52f8b3-a73f-47eb-8cc6-a5fa40959508/scratchpad/mockups/test/sw-fix (measure.sh, swiss.css, measure-light.log, shots/) so the reviewer's test/sw files stay untouched; validate_css.py swiss -> 0 problems. Frames measured: s1-l1/l2/l3 light, s2-l4 light, s2-l5 light + dark. No CLIPPED lines, no TITLE OVERFLOW, no CARD BELOW FRAME, no radius/shadow/background-image offenders, weights 400/500/700 only, colours limited to the two palettes (plus the three traffic-light colours).

Per violation, what was done and what the harness measured:
1. Tree fit: `.tree .cloud .meta, .tree .conflict .meta {display:none}` replaces the 11px rule. Rows 12/13 now nameCW = nameSW = 182 (was 122/163 and 106/138); no row of 14 truncates in any sidebar frame. The 16px-indent + 10px-caps alternative was computed and does not fit (needs ~270px of 207). Header comment corrected to say exactly this.
2. Title: centred on the WINDOW (position:absolute; left:0; right:0; line-height:38px inside a position:relative titlebar) in ink, 13px/500. With the base's flex centring the title would centre on the space right of the traffic lights (34px off the window centre), which is not how macOS draws it. Measured: titleBox 0-1280, align=center, colour rgb(11,11,11). Title kept visible rather than hidden: it carries the document name, which the owner will want to see in a comparison.
3. Baseline grid: `.line.h1 {line-height:48px}` (h1 h=48 in every editor frame), `.layout-l1 .editor {padding-top:48px}`. Editor slacks now: l2/l3 5px, l1 17px (the violation's predicted values). Board head: `height:72px` gives h=72.0 exactly; columns start at y=170 in l4 (pane top 74 + 96) and y=134 in l5 (38 + 96), so the column heads sit 5 baselines below the pane top. I did NOT use `align-items:center` as suggested: centring puts the 12px `.board-sub` ~6px above the 28px name's baseline and lands the 44px name box on a half pixel (6.5px). Instead `align-items:flex-start` with `align-self:baseline` on `.board-name` and `.board-sub` keeps name and sub on one baseline (the base's behaviour) while the buttons sit flush at the head's top edge; verified visually in the l4 and l5 screenshots. The now-dead `padding-bottom:12px` was removed since the explicit height governs. Also `.split-note .line.h1 {line-height:40px}` (2 x the split's 20px grid) — the violation's own slack arithmetic ("l5 50 -> 38px") presupposes this change even though its fix text only names the main h1.
4. `.lang {color:var(--ink)}`; fence markers stay grey.
5. `.split-board .board-sub {display:none}` deleted; measured board-head sw = cw = 538 in l5 light and dark, so the sub fits without overflow. The UTF-8 hide stays (see deviations).
6. `.checkbox {width:7px; height:7px; margin:0 7px 0 3px; vertical-align:1px}`; measured 7.0x7.0 open (1px ink border) and done (ink fill).
7. Split columns: `.split-board {flex:1.5}` -> column content 115.7px (was 111.2), "Wochenrückblick" (112.5px) fits and TITLE OVERFLOW is gone; `overflow-wrap:anywhere` added as the safety net for longer compounds. Consequence for the note pane: it narrows from 426 to 409px, line 13 ("Keine Anmeldung, ...") now wraps too, and with the 40px h1 the measured editor slack is 18px (not the 50/38 the violation predicted, which ignored the extra wrap) — still fits, verified in light and dark.
8. Column 1 axis: `.col-1 {border-left:0; padding-left:24px}` and `.split-board .col-1 {padding-left:12px}`. Note the values differ from the violation's suggested 25px/13px on purpose: with the base's border-box sizing, removing the 1px border and adding 1px of padding cancel out and the text would stay at x=297. Measured contentLeft of column 1 is 296.0 in l4 and l5, equal to the board-head's left edge (296.0); columns 2-4 unchanged. The selected-card bar got matching col-1 offsets (-24px / -12px) so it still lands on the column axis if a card in column 1 is selected (no such card in this content; the col-2 bar measures barX = colLeft in both boards).

Still open / unchanged and not flagged: the tabs row is 36px (1.5 baselines), so in l2/l3/l4 the content pane starts at y=74 and the grid is anchored to the pane top rather than to the body top; the status bar and sidebar foot are 24px on the last baseline. Dark palette and the #F3F3F3 code field remain derived values as declared in the header.

## Deviations
1. `.style-swiss .tree .cloud .meta` and `.style-swiss .tree .conflict .meta` are hidden (display:none). The words "nur online" / "Konfliktkopie" are removed from the visible DOM on the two badge rows. Reason: at 13px with the 24px depth-1 indent, the name plus badge plus meta word cannot fit in the 223px sidebar content width (163px name + badge + 58px meta > 199px available); the same state is carried by the badge glyph (with the title attribute) and by the sidebar-foot hint "2 Notizen nur online · 1 Konfliktkopie". This hide is the one the violation itself proposed.
2. `.style-swiss .split-note .status-item:nth-child(3)` (UTF-8) is hidden in layout l5. Reason: the split note pane is 409px wide (361px inside the 24px paddings); the three remaining items already span the full 361px (894.8-1256.0), and showing UTF-8 would need ~56px more (about 29px overflow, worse than the 10px measured at the old 426px width). A smaller gap cannot recover that, so hiding the least informative item is the honest fix. The other status bars show all four items.
3. Board head alignment uses flex-start plus a baseline-shared name/sub instead of the suggested align-items:center (see notes 3) — same measurable outcome (72px head, integer grid-aligned column start) with baseline-true typography.
4. `.col-1` padding-left is 24px (12px in the split), not the 25px/13px named in the violation, because 25/13 would reproduce the 1px offset the violation is about (measured 296.0 = head axis with 24/12).
5. Code font is ui-monospace / SF Mono / Menlo: the style defines no monospace face (pre-existing, declared in the header). Dark palette values are the spec's derived set, not catalog data (pre-existing, declared in the header).

## Critic summary
Verdict: no high-severity problems. The stylesheet is clean on scoping (validate_css.py swiss: 0 problems; only scoped selectors, no :root/html/body, no @import/url()/filters, the single @media is prefers-reduced-motion with scoped inner selectors), palette (every hex is a spec value for its mode; the only additions are the declared #F3F3F3 code surface and the native traffic-light colours; in dark frames #E30613 appears only as 2px bars / 7px squares — 3 uses in s1-l2-dark — and #FF4A55 carries all red text), depth/texture (a computed-style sweep of every element in all 10 frames found zero box-shadow, zero background-image, zero border-radius except the traffic lights), fonts/weights (computed weights used: 400/500/700 only; Inter loaded), motion (110ms linear, colour/border-colour only) and status coding (filled red 7px = In Arbeit, hollow grey = Ideen/Warten, ink square with 3px inset = Fertig, all with the word; no green/amber except the native traffic lights — a conflict between the CHROME RULE and the STATUS RULE that the author resolved in favour of native chrome, which I consider the defensible reading).

Fit was measured, not estimated: headless Chrome at 1280x800 with Inter loaded, all 5 layouts x light/dark. Status bar sits at y=776–800 in every frame; editor shows all 26 lines with 17px slack (l2, l3), 41px (l1), 50px (l5 split at 14/20px); the 72ch measure is 726.8px with the axis hairline 12px to its right and a 245px (24%) empty column; all 9 cards fit (lowest card bottom 502 in l4, 574 in l5); tabs and status bars never clip; the selected-card bar and active-result bar land exactly on their axes (x=504 = column hairline; x=41 inside the palette frame); sidebar-foot and status bar share the y=776 hairline. Contrast (computed): grey 5.10:1 on paper, 6.88:1 dark; red text 4.88:1 light, #FF4A55 5.72:1 dark; ink 19.7/16.1:1; only soft spot is grey on the #F3F3F3 code surface at 4.60:1.

Real problems found: one medium (two of fourteen tree rows truncate their file names, contradicting the header comment's claim about the 256px sidebar) and a handful of lows (title-bar alignment vs the native-chrome rule, off-baseline h1/l1 padding/board head, grey label on code surface below the spec's 5.1:1 floor, an unnecessary content hide in the split board head plus an undeclared but justified one in the split status bar, 9px checkbox vs the spec's 7px, split card titles at their width limit, and a 1px axis offset on column 1). Not listed as a violation but worth adding: the spec's 2px red focus ring has no :focus-visible rule (invisible in static frames). Artifacts: test pages, dumps, screenshots and measure logs are under /private/tmp/claude-501/-Users-sgrundhoefer-Projects-novalisNeo/fc52f8b3-a73f-47eb-8cc6-a5fa40959508/scratchpad/mockups/test/sw/ (swiss.css, measure.sh, dump-only.sh, shots/*.png, measure-light.log, measure-dark.log).

## Violations found by the critic
- [medium/fit] .style-swiss .sidebar (256px) / .style-swiss .tree .cloud .meta, .style-swiss .tree .conflict .meta / .style-swiss .tree .depth-1{padding-left:24px}: In every frame with a sidebar, tree rows 12 and 13 truncate their names: "Sync Strategies Compared" needs 163px but gets 122px (renders "Sync Strategies C…"), "Zettelkasten and PARA" needs 138px but gets 106px ("Zettelkasten an…"). The 24px depth-1 indent plus badge (11px) plus the 11px meta word ("nur online" / "Konfliktkopie") leaves too little of the 223px content width. The header comment's claim that 256px lets the longest file names survive at 13px is false for exactly the two rows that carry a badge; the meta word duplicates what the badge and the .sidebar-foot hint already say. → Drop or shorten the redundant meta on badge rows, e.g. `.style-swiss .tree .cloud .meta,.style-swiss .tree .conflict .meta{display:none}` (badge title + foot hint carry the state), or reduce `.tree .depth-1` to 16px and the badge-row meta to 10px caps; re-measure so nameSW <= nameCW on all 14 rows, then correct the header comment.
- [low/spec] .style-swiss .title{text-align:left;color:var(--grey)}: The CHROME RULE and the spec ask for a native macOS title bar with no custom chrome. A left-aligned grey 13px/500 title sitting next to the traffic lights reads as a custom toolbar label; native macOS centres the window title (or, with hiddenInset, shows none). The Swiss 'Kein Zentriersatz' rule is about content, not OS chrome. → Either keep the base `text-align:center` (native look) or hide the title entirely (`.style-swiss .title{visibility:hidden}`) to mimic hiddenInset; if kept, drop the grey and use ink so it does not look like secondary UI text.
- [low/spec] .style-swiss .line.h1{line-height:36px} / .style-swiss.layout-l1 .editor{padding-top:36px} / .style-swiss .board-head (measured 65.5px): The spec's 24px baseline grid and 'Blockabstände in Vielfachen von 24px' are broken in three places: the h1 line is 36px, so every editor line after line 5 sits 12px off-grid (measured h1 h=36 vs 24 for all other lines); the l1 editor top padding is 36px; the board head measures 65.5px (base align-items:baseline aligning 44px buttons with the 44px-line-height name) so the column heads start at y=163.5, a half-pixel and off-grid. → `.style-swiss .line.h1{line-height:48px}` (fits: l2/l3 slack 17->5px, l5 50->38px), `.style-swiss.layout-l1 .editor{padding-top:48px}` (slack 41->17px with the h1 change), and `.style-swiss .board-head{align-items:center;height:72px}` (or 60px) so columns start on an integer, grid-aligned y.
- [low/contrast] .style-swiss .lang / .style-swiss .line.fence .marker on --surface #F3F3F3: Secondary grey #6E6E6E on the derived code surface #F3F3F3 measures 4.60:1 (computed) — above WCAG AA 4.5 but below the spec's own 5.1:1 floor for secondary text; the JSON label is 10px caps, the smallest text in the editor. Dark mode is fine (#9C9C9C on #1A1A1A = 6.34:1). → `.style-swiss .lang{color:var(--ink)}` (micro caps in ink, as the demo's folio does); leave the fence markers grey (decorative). Lightening the surface does not reach 5.1 (#F7F7F7 gives 4.76:1).
- [low/spec] .style-swiss .split-board .board-sub{display:none} / .style-swiss .split-note .status-item:nth-child(3){display:none}: Both rules remove content from the shared DOM in layout l5. Probe: with .board-sub shown the board head still fits (scrollWidth 521 = clientWidth 521, no wrap), so that hide is unnecessary; with UTF-8 shown the split status bar overflows by 10px (436 vs 426), so that hide is justified — but neither is a styling decision the owner can see, and both must appear in the deliverable's deviations. → Restore .board-sub in the split (delete the rule). Keep the UTF-8 hide and declare it in deviations (gap 12px would still leave ~2px overflow, so hiding is the honest fix).
- [low/spec] .style-swiss .checkbox{width:9px;height:9px}: The spec's editor translation says '@due tasks: 7px square checkbox (filled = done)'; every other form mark in the sheet (.state-mark, .status-dot, .hint-dot) is 7px. A 9px checkbox is a second square size in a style whose vocabulary is one square. → `.style-swiss .checkbox{width:7px;height:7px;margin:0 7px 0 3px;vertical-align:1px}` so the task square matches the status squares.
- [low/fit] .style-swiss .split-board .column{padding:0 12px} + .style-swiss .split-board .card-title{font-size:14px}: Split columns have a 111.2px content box; the unhyphenated word 'Wochenrückblick' measures 112.5px and overflows by 1.3px into the gutter (invisible with this content, but the column is at its limit — any longer German compound crosses the 12px gutter into the neighbouring hairline). Hyphenated titles ('Konfliktkopien-Regex') break at the hyphen and are fine. → `.style-swiss .split-board .card-title{overflow-wrap:anywhere}` as a safety net, or widen the board side (`.style-swiss .split-board{flex:1.5}` gives ~120px per column) — the note pane still has 50px of editor slack at 14/20px.
- [low/spec] .style-swiss .col-1{border-left-color:transparent}: Column 1 keeps its 1px border (made transparent), so its text axis is at x=297 while the board name and the 3px heavy rule start at x=296 — a 1px axis mismatch in a style whose rule is 'Kanten bündig zur Spaltenachse'. Columns 2–4 and the heavy rule's right end (1176 = col-4 content edge) are exact. → `.style-swiss .col-1{border-left:0;padding-left:25px}` (and `.style-swiss .split-board .col-1{padding-left:13px}`) so column 1's content starts at exactly the board-head axis.
